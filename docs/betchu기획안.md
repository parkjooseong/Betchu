# BETCHU — 우리 둘의 베팅 몬스터

## 서비스 기획안 v0.1

이 문서는 제품 규칙과 구현 계약을 함께 관리한다. [README](../README.md)에 요약된 현재 확정 MVP 흐름을 상위 기준으로 삼고, 이 문서의 세부 상태·데이터 구조·API 계약은 그 흐름과 충돌하지 않도록 동기화한다. 결정 상태와 출시 범위를 다음 표기로 구분한다.

# 0. 문서 표기 규칙

| 구분 | 표기 | 의미 |
| --- | --- | --- |
| 결정 상태 | `[확정]` | 구현과 검수 기준으로 채택한 규칙 |
| 결정 상태 | `[제안]` | 사용자 검증이나 추가 결정이 필요한 규칙 |
| 출시 범위 | `[MVP]` | 1차 버전에 포함 |
| 출시 범위 | `[후속]` | 1차 버전에서 제외 |
| 출시 범위 | `[공통]` | 버전과 무관한 안전·데이터 원칙 |
| 개발 우선순위 | `P0` | MVP 출시 전에 반드시 완료해야 하는 기능·품질 요건 |
| 개발 우선순위 | `P1` | MVP 출시 후 첫 번째 개선 주기에 반영할 후속 기능 |
| 개발 우선순위 | `P2` | P1 검증 후 경제·악용 안전장치를 갖춘 두 번째 후속 확장 |

가장 가까운 상태 표기는 다음 상태 표기 전까지 이어지는 하위 표·목록·예시에 적용한다. 상태 표기가 없는 문장은 배경 설명이며 구현 요구사항으로 사용하지 않는다.

| 문서 구간 | 결정 상태 | 출시 범위 |
| --- | --- | --- |
| 1~18절 핵심 규칙 | `[확정]` | `[MVP]` (명시된 후속 기능 제외) |
| 19절 정식 PvP | `[제안]` | `[후속]` |
| 20절 간이 협동 보스 | `[확정]` | `[MVP]` |
| 20절 정식 레이드 | 전투·보상 `[제안]`, 4주 보스 순환 `[확정]` | `[후속]` |
| 21절 월간 시즌 | `[제안]` | `[후속]` |
| 22~32절 화면·정책·구현 계약 | 각 절·표의 로컬 표기 기준 | `[MVP]`, `[후속]`, `[공통]` 로컬 표기 기준 |
| 33절 개발 순서 | `[제안]` | 단계별 표기 |
| 34절 MVP 지표 | `[확정]` | `[MVP]` |
| 34절 후속 지표·35절 장기 비전 | `[제안]` | `[후속]` |

BETCHU의 핵심은 서로의 약속에 코인을 걸고, 약속을 지킨 만큼 각자의 몬스터를 성장시키며 아이템과 전투 기록을 쌓는 것이다. 약속 생성부터 베팅, 결과 확인, 뽑기, 장비, 전투까지 하나의 반복 구조로 연결된다.

---

# 1. 프로젝트 개요

| 항목     | 내용                         |
| ------ | -------------------------- |
| 프로젝트명  | **BETCHU**                 |
| 한글 발음  | **배츄**                     |
| 부제     | **우리 둘의 베팅 몬스터**           |
| 장르     | 커플 약속 게임 + 몬스터 육성 + 아이템 수집 |
| 이용 인원  | 연결된 커플 2명                  |
| 핵심 문구  | **약속하고, 걸고, 키운다.**         |
| 주요 플랫폼 | iOS·Android 모바일 앱          |
| 핵심 타깃  | 만 14세 이상, 게임을 가볍게 즐기는 10대 후반~20대 커플 |
| 수익 구조  | 초기에는 없음, 추후 꾸미기 상품 중심      |
| AI API | 사용하지 않음                    |
| 실제 현금  | 코인 구매·환전·사용자 간 송금 없음       |

## 한 줄 소개

> **일상의 약속에 코인을 걸고, 성공한 만큼 나만의 몬스터를 키우는 2인용 커플 게임**

---

# 2. 서비스가 해결하는 문제

기존 커플 앱은 사진, 디데이, 일정처럼 **기록 중심**인 경우가 많다. 한동안 사용한 뒤에는 새롭게 할 행동이 부족해질 수 있다.

반면 습관 관리 앱은 목표 달성에는 도움이 되지만, 커플끼리 장난치고 경쟁하거나 함께 성장한다는 느낌이 약하다.

BETCHU는 이 둘을 결합한다.

| 일반적인 행동       | BETCHU에서의 표현 |
| ------------- | ------------ |
| “오늘 1시 전에 잘게” | 퀘스트 생성       |
| 약속에 자신감 보이기   | 코인 걸기        |
| 상대방이 조건과 결과 예상 확인 | 파트너 시작 승인 |
| 실제로 약속 지키기    | 퀘스트 수행       |
| 상대방이 실제 결과 확인 | 파트너 판정·최종 승인 |
| 보상 받기         | 최종 정산 후 코인·경험치 획득 |
| 꾸준히 지키기       | 계정 레벨업·성공 누적 외형 성장 |
| 번 코인 사용       | 뽑기·아이템 구매    |
| 서로 비교하기       | 전투력·연승·시즌 기록 |
| 함께 노력하기       | MVP 간이 협동 보스  |

즉, **실제 생활에서의 행동이 게임 속 성장으로 바뀌는 것**이 BETCHU의 가장 중요한 차별점이다.

---

# 3. 서비스 목표

## 핵심 목표

1. 커플이 하루에 한 번 이상 자연스럽게 상호작용하게 만든다.
2. 사소한 약속도 게임의 퀘스트처럼 즐길 수 있게 만든다.
3. 약속을 지킨 결과가 몬스터와 아이템으로 눈에 보이게 남도록 한다.
4. 경쟁과 협동을 적절히 섞어 한 사람만 계속 지는 구조를 방지한다.
5. 몇 달 동안 사용한 기록이 둘만의 성장 기록이 되게 만든다.

## 하지 않을 것

초기 버전에서는 다음 기능을 넣지 않는다.

* 실제 돈으로 코인 구매
* 코인의 현금 환전
* 다른 커플과 공개 경쟁
* 위치·카카오톡·휴대폰 사용 시간 자동 감시
* AI를 이용한 약속 판정
* 확률형 유료 상품
* MVP의 사용자 간 아이템 교환·선물 (후속 P2의 현재 커플 한정 기능만 예외)

앱의 느낌도 카지노나 도박보다는 **다마고치·캐주얼 RPG·커플 퀘스트 게임**에 가깝게 만든다.

---

# 4. 핵심 용어

| 용어     | 의미                              |
| ------ | ------------------------------- |
| 퀘스트    | 커플이 등록한 실제 생활 속 약속              |
| 도전자    | 해당 약속을 수행하는 사람                  |
| 파트너    | 약속 내용과 결과를 확인하는 상대방             |
| 츄코인    | 퀘스트에 걸고 아이템 구매에 사용하는 가상 코인      |
| 배츄     | 각 플레이어가 키우는 몬스터                 |
| 정산     | 약속 성공·실패에 따라 코인과 경험치를 처리하는 과정   |
| 전투력    | 몬스터의 최종 HP·ATK로 서버가 계산하는 수치        |
| 케미 게이지 | 유효한 일반 개인 퀘스트가 파트너 최종 승인으로 성공·실패 정산될 때 채워지는 협동 게이지 |
| 시즌     | 한 달 단위로 기록을 정리하는 기간             |

앱 내부에서는 `베팅`이라는 표현을 계속 반복하기보다 다음처럼 가볍게 표현하는 것이 좋다.
단, 배츄는 몬스터를 공통적으로 칭하는 호칭이고 각자의 캐릭터 이름은 스타팅 배츄 선택 시 원하는 이름을 입력받아 사용한다.
배츄 이름은 앞뒤 공백을 제거한 뒤 1~10자로 제한하고, 빈 문자열·개행·제어 문자는 허용하지 않는다. 다른 사용자의 배츄와 같은 이름은 사용할 수 있다.

> “100코인 걸고 도전할래?”
> “오늘은 어떤 퀘스트를 걸어볼까?”
> “결과를 확인해야 배츄가 성장해!”

---

# 5. 전체 핵심 게임 루프

```text
커플 연결
   ↓
각자 스타팅 배츄와 이름 선택
   ↓
퀘스트 만들기
   ↓
코인과 난이도 설정
   ↓
파트너의 성공·실패 예상 선택과 시작 승인
   ↓
퀘스트 수행·파트너 예상 표시
   ↓
파트너의 실제 결과 입력과 최종 승인
   ↓
성공·실패 정산 또는 무효 처리
   ↓
뽑기·아이템 장착
   ↓
몬스터 성장·진화
   ↓
파트너 성장 비교·MVP 간이 협동 보스
   ↓
더 높은 보상의 퀘스트 도전
```

MVP에서는 평일에 전투력·연승·주간 성장량을 비교하고, 주말에는 두 몬스터의 합산 전투력으로 간이 협동 보스를 판정한다. 선택형 PvP와 정식 레이드는 후속으로 확장한다.

> **경쟁 50% + 협동 50%**

이 비율을 유지해야 한 사람이 계속 뒤처지더라도 게임을 함께 계속할 이유가 생긴다.

---

# 6. 최초 가입과 온보딩

## 6.1 회원가입

> 결정 상태: `[확정]` · 출시 범위: `[MVP]`

사용자는 간편 로그인을 통해 가입한다.

```text
BETCHU

약속하고, 걸고, 키운다.

[Google로 시작하기]
[Apple로 시작하기]
[카카오로 시작하기]
```

국내 MVP는 서비스 정책상 **만 14세 이상만 가입 가능**하게 한다. 가입 시 만 14세 이상 여부, 이용약관, 개인정보 처리방침과 선택적인 인증 자료 처리 동의를 구분해 확인하며, 동의 문서의 버전과 시각을 저장한다. 생년월일 전체는 저장하지 않고 연령 기준 통과 여부만 저장한다. 사용자가 만 14세 미만이라고 응답하거나 연령 확인을 거절하면 제한 세션을 즉시 폐기하고 임시 가입자·OAuth 프로필 데이터는 24시간 이내 삭제한다. 해외 출시 전에는 국가별 최소 연령과 보호자 동의 요건을 별도로 검토한다.

## 6.2 커플 연결

> 결정 상태: `[확정]` · 출시 범위: `[MVP]`

먼저 가입한 사람이 초대 코드를 만든다.

```text
내 커플 코드

B7CHU-9K4M-X2Q8

[코드 복사]
[초대 링크 보내기]
```

초대 코드는 추측하기 어려운 12자 이상의 암호학적 난수로 만들고 생성 후 24시간 동안 한 번만 사용할 수 있다. 서버 비밀키 기반 HMAC만 저장하며 원문은 생성 응답에서 한 번만 보여준다. 새 코드를 만들거나 사용자가 철회하면 기존 코드는 즉시 무효화한다. 사용자당 `ACTIVE` 또는 `PENDING_CONFIRMATION` 초대는 하나만 허용하며 자기 코드 사용, 이미 연결된 계정, 차단 관계의 연결을 막는다. 코드 입력 시도에는 횟수 제한을 적용한다.

상대방이 코드를 입력하면 `couple_invites`를 `PENDING_CONFIRMATION`으로 바꾸고 즉시 연결하지 않는다. 이 초대의 `id`를 연결 요청 ID로 사용한다. 두 사람 모두 서로의 닉네임과 프로필 이미지를 확인하고 코드 생성 시각부터 24시간 안에 독립적으로 최종 동의해야 한다. 한쪽이 거절하거나 기한이 지나면 요청과 코드를 무효화하며, 서버 만료 작업도 같은 요청에 중복 적용되지 않게 처리한다. 두 번째 확인 요청에서 첫 번째 확인, 만료 여부, 차단 관계와 양쪽의 활성 멤버십 부재를 다시 검사한 뒤 커플·멤버십 생성과 초대 `USED` 전환을 한 트랜잭션으로 처리한다.

## 6.3 스타팅 배츄 선택

커플 연결이 완료되면 각자 알 하나를 받고 스타팅 배츄를 선택한다. 시작 능력치는 동일하게 두고 종별 외형·색상만 다르게 한다. 각자의 1,000C는 커플 연결이나 재연결 때가 아니라 6.1절의 가입 활성화 시 계정당 한 번만 지급한다.

| 스타팅 배츄 | 색상·외형 방향 | MVP 성능 | 서버 코드 |
| --- | --- | --- | --- |
| 별빛 | 노랑·보라, 밤하늘과 별빛 느낌 | 동일 | `STARLIGHT` |
| 파도 | 하양·파랑, 말랑한 파도와 바다 느낌 | 동일 | `WAVE` |
| 노을 | 빨강·갈색, 따뜻한 노을과 땅 느낌 | 동일 | `SUNSET` |
| 숲 | 초록·갈색, 포근한 나무와 숲 느낌 | 동일 | `FOREST` |

스타팅 배츄의 종류와 이름을 함께 선택한다.

```text
배츄 이름
[이름 입력 (1~10자)]
```

```text
LV.1
HP 100
ATK 10
장비 0/3
츄코인 1,000C
```

공통 알 단계는 종 선택 전용 표식만 보여주고, 첫 부화부터 선택한 종의 팔레트를 적용한다. 스타팅 간 능력치 차이는 두지 않는다. MVP에는 알·아기·중간 성장·최종 진화의 4단계 정적 외형을 포함하고, 종별 전용 스킬·분기 진화·추가 진화 형태는 후속 버전에서 확장한다.

## 6.4 튜토리얼 퀘스트

첫 퀘스트는 시스템이 안내한다.

```text
첫 번째 퀘스트

오늘 자기 전에 물 한 잔 더 마시기

난이도 ★
걸기 100C

[파트너에게 보내기]
```

튜토리얼은 사용자당 한 번만 제공한다. 시작 승인 때 실제 100C를 잠가 지갑 흐름을 보여주지만 일일 코인 퀘스트 횟수와 보너스 예산에는 포함하지 않는다. 파트너가 성공으로 최종 승인하면 원금과 최초 1회 보너스 100C, 10XP를 지급한다. 실패로 최종 승인하거나 최종 승인을 거절·누락해 `INVALID`가 되거나 관계가 종료되면 원금만 반환하며 보너스·XP·연승·케미는 지급하지 않는다.

튜토리얼이 최종 `SUCCESS`로 정산되면 같은 트랜잭션에서 스타팅 배츄를 `EGG → BABY`로 즉시 부화시킨다. 튜토리얼 성공은 15절의 영구 인정 성공 수에는 포함하지 않는다. 튜토리얼이 `FAILURE`·`INVALID`·관계 종료로 끝나면 튜토리얼을 반복하지 않고, 이후 첫 일반 개인 퀘스트 `SUCCESS` 정산 시 부화시킨다.

## 6.5 연결 종료·차단·탈퇴

> 결정 상태: `[확정]` · 출시 범위: `[MVP]`

연결 종료는 안전 기능이므로 상대방의 동의를 요구하지 않는다. 어느 한쪽이 연결 종료 또는 차단을 실행하면 상대 프로필, 공동 화면, 상대가 제출한 인증 자료에 대한 접근을 즉시 차단한다.

연결 종료 요청은 안전 차단과 코인 정산을 두 단계로 처리한다. 첫 트랜잭션은 커플을 `ENDED`로 바꾸고 양쪽 `couple_members`를 종료 처리해 `left_at`을 기록한다. 또한 모든 관계 기반 조회·알림을 차단하고, 판정 전인 해당 주간 간이 협동 보스를 `CANCELED_RELATIONSHIP_ENDED`로 종료한 뒤 고유한 `relationship_end_jobs`를 기록하며 즉시 커밋한다. 모든 관계 자원은 동일한 `couples` 행을 첫 잠금 순서로 처리한다. 따라서 뒤의 코인 정산 일부가 실패해도 접근 차단은 롤백되지 않는다. 연결 종료 자체는 코인 정산 결과를 기다리지 않고 항상 허용한다. 종료 멤버십은 활성 멤버십 고유 제약을 더 이상 점유하지 않지만, 이전 `relationship_end_jobs`가 `COMPLETED`가 되기 전까지는 새 초대 참여와 연결 확정을 거절한다.

후속 정산 작업은 아래 규칙을 퀘스트별 멱등 트랜잭션으로 적용하고 실패한 건만 재시도한다.

1. `DRAFT`, `PENDING_APPROVAL`, `CHANGE_REQUESTED`인 퀘스트는 커플·퀘스트 행을 잠근 뒤 별도 코인 반환 없이 `CANCELED_RELATIONSHIP_ENDED`로 종료한다. 이미 `APPROVAL_EXPIRED`된 퀘스트는 추가 정산하지 않는다.
2. 수행 마감 전 진행 중 퀘스트는 `CANCELED_RELATIONSHIP_ENDED`로 종료하고 잠긴 코인을 원래 소유자에게 반환한다.
3. 수행 마감 후 `AWAITING_RESULT` 또는 `PENDING_FINAL_APPROVAL`인 퀘스트는 파트너 최종 승인이 아직 커밋되지 않은 상태이므로 `INVALID`, `invalid_reason = RELATIONSHIP_ENDED_BEFORE_FINAL_APPROVAL`로 정산하고 잠긴 원금을 반환한다. 종료 전에 파트너 최종 승인과 정산이 이미 커밋된 `SUCCESS`·`FAILURE`만 그대로 유지한다.
4. 마감 전 취소와 마감 후 `INVALID`는 성공 보너스·경험치·인정 성공 수·케미를 지급하지 않고 연승을 유지한다. 파트너가 실패로 최종 승인해 이미 커밋된 `FAILURE`만 연승을 0으로 초기화한다.
5. 각 사용자의 몬스터·아이템·개인 지갑은 유지한다.
6. 공동 기록은 화면에서 숨기고 보안·정산 이의 처리에 필요한 최소 메타데이터만 접근 제한 상태로 보관한다.

P2 교환·선물·정식 레이드가 열린 뒤에는 연결 종료·차단 직후 해당 커플의 미완료 교환·선물·레이드도 더 이상 조회·수락·확정할 수 없다. `relationship_end_jobs`의 후속 단계가 이를 `CANCELED_RELATIONSHIP_ENDED`로 한 번만 종료하고, 교환·보유 아이템 선물 예약과 미완료 주간 예약 횟수를 해제한다. 상점 선물로 잠긴 코인은 전액 반환 원장을 남기며, 정식 레이드는 보상 없이 종료한다. 이미 완료된 이전·선물·레이드 보상은 되돌리지 않는다.

종료 화면은 관계 종료 작업에 스냅샷된 모든 자원의 정리가 끝날 때까지 `PROCESSING`을 표시한다. MVP에서는 대상 퀘스트 정산·간이 협동 보스 종료가, 해당 기능 출시 뒤에는 교환·선물·정식 레이드 정리까지 모든 `relationship_end_job_items`가 `COMPLETED`이고 `processed_resource_count = target_resource_count`일 때만 상위 작업을 `COMPLETED`로 바꾼다. 화면은 전체 대상·처리 수와 관계 정리 상태를 보여주고, 완료 뒤 최종 사용 가능·잠긴 잔액을 함께 표시한다.

연결 종료 직후부터 어느 쪽도 상대 프로필과 상대가 제출한 자료를 볼 수 없다. 인증 자료는 백엔드 미디어 게이트웨이가 요청마다 권한을 검사해 전달하므로 종료 전에 얻은 영구·직접 저장소 URL로 접근할 수 없다. 각 사용자는 종료 시점에 아직 남아 있는 자신이 작성한 퀘스트 텍스트와 자신이 제출한 인증 처리본만 내보낼 수 있으며 상대 식별 정보는 가린다. 내보내기 기한은 `min(원래 최종 정산 시각 + 30일, 연결 종료 시각 + 30일)`이고 삭제·동의 철회 요청이 더 빠르면 즉시 차단 후 삭제 절차를 적용한다. 일반 공동 기록과 관계 식별자는 30일 이내 삭제 또는 익명화하고, 지갑 원장·신고처럼 별도 보관 근거가 있는 최소 데이터만 분리 보관한다.

차단은 연결 종료를 포함하며 이후 초대·재연결·상대방 알림을 모두 막는다. 신고 사실과 신고 내용은 상대방에게 공개하지 않는다.

계정 탈퇴는 재인증 후 세션을 즉시 폐기하고, 연결 종료와 진행 중 퀘스트 정산을 먼저 수행한다. 일반 인증 자료 저장본은 즉시 접근 차단 후 7일 이내 삭제하고, 그 밖의 일반 개인정보는 30일 이내 삭제 또는 익명화한다. 부정 사용 방지·신고 처리·법적 의무에 필요한 최소 원장과 신고 기록 및 신고 자료 보존본은 일반 서비스 데이터와 분리해 권한과 기한을 제한한다. 사용자는 탈퇴 전에 자신의 데이터를 내보낼 수 있다.

---

# 7. 퀘스트 시스템

## 7.1 퀘스트 생성 항목

| 입력 항목    | 예시                |
| -------- | ----------------- |
| 퀘스트 제목   | 오늘 새벽 1시 전에 자기    |
| 카테고리     | 취침                |
| 성공 조건    | 오전 1시 이전에 잠자리에 들기 |
| 난이도      | ★★                |
| 걸 코인     | 100C              |
| 수행 마감    | 오늘 오전 1시          |
| 최소 수행 시간 | 30분               |
| 승인 마감    | 오늘 오전 12시 30분    |
| 결과 확인 시작 | 내일 오전 9시          |
| 최종 승인 마감 | 확인 시작 후 24시간      |
| 인증 방식    | 사용 안 함 또는 선택 사진  |

`approval_deadline_at`은 서버가 `due_at - minimum_duration_minutes`로 계산한다. 기본 템플릿의 최소 수행 시간은 서버 템플릿에 고정하고, 자유 입력 퀘스트는 도전자가 허용 범위 안에서 제안해 현재 버전에 포함한다. 시점형 행동은 `0분`을 허용한다. 제출과 승인은 모두 서버 시각이 `approval_deadline_at`보다 빠를 때만 허용한다. 제출 시 이미 마감과 같거나 늦으면 새 버전을 만들지 않고 `APPROVAL_DEADLINE_NOT_FUTURE`를 반환하며 `DRAFT` 또는 `CHANGE_REQUESTED`를 유지해 작성자가 수행 마감 등을 고칠 수 있게 한다. 이미 `PENDING_APPROVAL`인 요청은 마감 도달 시 `APPROVAL_EXPIRED`로 종료한다.

`result_at`은 수행 마감 이후 파트너의 결과 확인이 시작되는 시각이고, `result_confirmation_deadline_at`은 서버가 `result_at + 24시간`으로 계산한다. 파트너의 실제 결과 선택과 최종 승인은 모두 이 기한 전에 끝나야 하며 사용자가 임의로 늘리거나 줄일 수 없다.

## 7.2 퀘스트 카테고리

* 취침
* 운동
* 공부
* 연락
* 게임
* 소비
* 집안일
* 식습관
* 데이트
* 자유 입력

카테고리를 저장해두면 사용자의 과거 성공률과 시즌 통계를 만들 수 있다.

```text
사용자 1의 게임 퀘스트 성공률
38%

사용자 2의 취침 퀘스트 성공률
82%
```

## 7.3 퀘스트 종류

### 개인 퀘스트

> 결정 상태: `[확정]` · 출시 범위: `[MVP]`

한 명이 수행하고 상대방이 확인한다.

> 오늘 1시 전에 자기

### 커플 퀘스트

> 결정 상태: `[제안]` · 출시 범위: `[후속]`

두 사람이 함께 수행한다.

> 이번 주 데이트 사진 5장 남기기

둘 다 성공해야 공동 보상을 받는다.

### 장기 챌린지

> 결정 상태: `[제안]` · 출시 범위: `[후속]`

며칠간 이어지는 목표다.

> 이번 주 월·수·금 운동하기

### 보스 퀘스트

> 결정 상태: `[확정]` · 출시 범위: `[MVP]`

MVP 간이 협동 보스를 해금하기 위한 시스템 공동 목표다.

> 이번 주 둘이 합쳐 유효한 일반 개인 퀘스트 10개 정산하기

MVP에서 사용자가 직접 만드는 퀘스트는 **개인 퀘스트만 구현**한다. 다만 개인 퀘스트 정산으로 채우는 시스템 공동 목표와 간이 협동 보스는 MVP에 포함한다. 커플 퀘스트·장기 챌린지·선택형 정식 레이드는 후속 버전으로 확장한다.

---

# 8. 파트너 시작 승인 시스템

퀘스트는 파트너가 시작 승인해야 수행할 수 있다. 파트너는 성공 조건과 난이도를 확인한 뒤 도전자의 성공 또는 실패를 예상해 하나를 선택하고 현재 버전을 승인한다. 이 예상은 게임 상호작용용 기록이며 성공 보너스·XP·최종 판정을 바꾸지 않는다. 코인 파밍은 승인만 믿지 않고 난이도별 베팅 상한과 일일 서버 발행 한도로 함께 막는다.

파트너에게 다음 화면이 전달된다.

```text
사용자 1이 새로운 퀘스트를 보냈어!

오늘 새벽 1시 전에 자기

버전         v2
난이도       ★★
걸기         100C
예상 성공 보너스 75C
성공 조건    오전 1시 이전 취침
수행 마감    오늘 오전 1시
최소 수행    30분
승인 마감    오늘 오전 12시 30분
결과 확인    내일 오전 9시 ~ 모레 오전 9시
인증 방식    선택 사진

v1에서 바뀐 항목
- 걸기 50C → 100C
- 성공 조건 문구 수정

성공할 것 같아 / 실패할 것 같아

[예상 선택 후 시작 승인]
[수정 요청]
[거절하기]
```

## 승인 규칙

| 상황 | 처리 |
| --- | --- |
| `DRAFT` | 현재 작업 초안은 작성자만 볼 수 있고 자유롭게 수정·폐기 가능. 코인·슬롯·보너스 예약 없음. 회수 전에 전달된 불변 버전은 아래 감사 규칙 적용 |
| 초안 폐기 | 작성 중인 `quest_drafts`를 삭제하고 `DISCARDED`로 종료. 한 번도 제출하지 않은 초안은 파트너에게 존재 자체를 공개하지 않음 |
| 첫 제출 | 현재 내용을 불변 `v1`로 고정하고 `PENDING_APPROVAL`로 전환 |
| `PENDING_APPROVAL` 회수 | 작성자만 회수해 `DRAFT`로 돌릴 수 있음. 해당 버전의 승인 요청은 무효 |
| 파트너 수정 요청 | `CHANGE_REQUESTED`로 전환하고 요청 문구를 기록 |
| 수정 후 재제출 | 직전 제출본과 다른 항목이 있어야 하며 `v1 → v2`처럼 버전을 올려 `PENDING_APPROVAL`로 전환. 이전 판단은 모두 무효 |
| 파트너 시작 승인 | 성공·실패 예상 중 하나를 선택하고 정확히 표시된 현재 버전만 승인. 예상 기록, `ACTIVE` 전환, 코인 잠금, 해당 결과 예정일의 슬롯·보너스 예약을 한 트랜잭션으로 처리 |
| 승인 마감 도달 | 내부 상태 `APPROVAL_EXPIRED`, 화면에는 `EXPIRED`. 코인 차감·잠금·슬롯·보너스 예약 없음 |
| 거절 | `REJECTED`로 종료. 코인 잠금 없음 |
| `ACTIVE` 후 수정 | 퀘스트 조건과 시작 예상 모두 수정 불가. 조건을 바꾸려면 수행 마감 전 양쪽 합의 취소 후 새 퀘스트를 생성 |
| 수행 마감 전 취소 요청 | 두 사람 모두 동의하면 취소 |
| 수행 마감 전 공동 취소 | 잠긴 코인 반환, 보너스·경험치·케미 없음 |
| 수행 마감 이후 사용자 취소 | 허용하지 않고 반드시 결과 판정 진행. 단, 안전 기능인 연결 종료·차단은 6.5절의 별도 정산 규칙 적용 |

제출된 각 버전은 수정·삭제하지 않고 보존한다. 파트너에게 보이는 `changedFields`는 클라이언트가 적어 내는 값이 아니라 서버가 직전·현재 불변 버전을 비교해 계산한다. `PENDING_APPROVAL`은 직접 수정할 수 없고 반드시 먼저 회수해야 한다. `CHANGE_REQUESTED` 수정 중에도 파트너는 미제출 작업 내용을 볼 수 없다.

일반 개인 퀘스트 초안은 튜토리얼 중에도 작성할 수 있지만, 도전자의 튜토리얼이 `SUCCESS`·`FAILURE`·`INVALID` 등 최종 상태로 끝나 `tutorial_completed_at`이 기록되기 전에는 제출할 수 없다. 이 경우 `TUTORIAL_REQUIRED`를 반환한다. 시작 승인 시에도 같은 조건을 다시 검사해 튜토리얼과 일반 성공의 부화 순서가 뒤바뀌지 않게 한다.

승인 대기 상태의 승인·수정 요청·거절·회수는 모두 퀘스트 행을 잠근 뒤 승인 마감을 먼저 검사한다. 서버 시각이 마감과 같거나 늦으면 스케줄러 도착 여부와 관계없이 `APPROVAL_EXPIRED`가 우선하며 다른 전이는 커밋하지 않는다. 회수·수정 요청 뒤 재제출할 때 직전 제출 버전과 서버 비교 결과가 완전히 같으면 `UNCHANGED_QUEST_VERSION`으로 거절한다.

처음부터 제출하지 않은 `DRAFT`의 존재와 내용은 작성자에게만 보인다. 이미 파트너에게 전달된 버전을 회수한 경우 파트너 화면에서는 활성 승인 항목을 제거하고 `회수됨` 감사 표시와 과거에 실제 전달된 불변 버전만 유지한다. 현재 `quest_drafts`의 내용·수정 시각·새 변경점은 다시 제출되기 전까지 공개하지 않는다. `CHANGE_REQUESTED`에서는 마지막 제출 버전과 파트너 본인의 수정 요청만 공유하고 작성 중 초안은 가린다. `ACTIVE` 이후 화면은 `approved_quest_version_id`를 기준으로 표시한다.

파트너에게는 퀘스트 제목·성공 조건·난이도·걸린 코인·해당 퀘스트의 예상 성공 보너스·수행 및 결과 시각·최소 수행 시간·승인 마감·인증 방식·현재 버전 ID와 번호·서버 계산 수정 항목만 공유한다. 시작 승인 때 선택한 `predicted_result`는 `ACTIVE` 전환과 함께 고정해 수행 기간 동안 도전자와 파트너 모두에게 보여주고 최종 결과와 별도로 보존한다. 도전자의 전체 가용·잠긴 잔액, 남은 개인 코인 퀘스트 횟수·보너스 예산, 전체 지갑 원장, 뽑기·구매 내역, 미제출 `DRAFT`는 공유하지 않는다. 작성자 전용 예산 견적 응답에서만 남은 개인 한도를 보여준다.

승인 순간 잔액·일일 한도가 부족하면 코인을 일부만 처리하지 않고 승인을 롤백한다. 파트너에게는 금액을 포함하지 않은 `QUEST_REQUIRES_REVISION`만 반환하고, 작성자에게만 사유와 수정 가능한 범위를 개인 알림으로 제공한다.

`POST /quests/{questId}/cancel`은 수행 마감 전에만 호출할 수 있고 취소 요청을 `PENDING`으로 기록할 뿐 퀘스트 수행과 시간 흐름을 멈추지 않는다. 요청자가 아닌 상대방이 `confirm-cancel`로 동의한 때만 공동 취소를 정산한다. 상대방이 거절하거나 `due_at`이 지나면 요청을 `REJECTED` 또는 `EXPIRED`로 닫고 정상 결과 판정으로 진행한다.

---

# 9. 퀘스트 상태

개발할 때는 퀘스트 상태를 명확하게 분리해야 한다.

```text
DRAFT
  ├─ discard ─→ DISCARDED
  │ submit(v1)
  ↓
PENDING_APPROVAL ── recall ──→ DRAFT
  │
  ├─ request-change ─→ CHANGE_REQUESTED
  │                         ├─ discard ─→ DISCARDED
  │                         │ edit + submit(v2)
  │                         └─────────→ PENDING_APPROVAL
  ├─ reject ─────────→ REJECTED
  ├─ approval deadline ─→ APPROVAL_EXPIRED
  │ predict + approve(exact version)
  ↓
ACTIVE (승인 버전·파트너 예상 수정 불가)
  ├─ mutual cancel before due ─→ CANCELED
  │ result_at
  ↓
AWAITING_RESULT
  ├─ partner selects SUCCESS or FAILURE ─→ PENDING_FINAL_APPROVAL
  ├─ partner rejects judgment ───────────→ INVALID
  └─ result confirmation deadline ───────→ INVALID

PENDING_FINAL_APPROVAL
  ├─ final approve selected SUCCESS ─→ SUCCESS
  ├─ final approve selected FAILURE ─→ FAILURE
  ├─ final reject ────────────────────→ INVALID
  └─ result confirmation deadline ────→ INVALID
```

예외 상태는 다음과 같다.

```text
REJECTED
DISCARDED
CANCELED
APPROVAL_EXPIRED
INVALID
CANCELED_RELATIONSHIP_ENDED
```

| 상태               | 의미          |
| ---------------- | ----------- |
| DRAFT            | 현재 작업 초안은 파트너에게 미공개. 회수된 경우 과거 전달본만 응답 전용 `auditProjectionStatus = RECALLED`로 표시 |
| PENDING_APPROVAL | 현재 불변 버전의 파트너 승인 대기 |
| CHANGE_REQUESTED | 파트너의 수정 요청 후 작성자 수정·재제출 대기 |
| ACTIVE           | 시작 승인된 버전과 파트너 예상을 표시하며 수행 중 |
| AWAITING_RESULT  | 결과 확인 시작 뒤 파트너의 실제 결과 선택 대기 |
| PENDING_FINAL_APPROVAL | 파트너가 실제 결과를 선택했고 최종 승인·거절 전인 상태 |
| SUCCESS          | 파트너가 성공으로 최종 승인해 성공 정산 완료 |
| FAILURE          | 파트너가 실패로 최종 승인해 실패 정산 완료 |
| INVALID          | 파트너의 판정 거절·최종 승인 거절·기한 만료 또는 서버 기술 오류로 무효 처리 |
| REJECTED         | 파트너가 현재 제출 버전을 거절해 승인 없이 종료 |
| DISCARDED        | 작성자가 승인 전 초안을 폐기해 종료 |
| APPROVAL_EXPIRED | 승인 마감 종료. 화면 표시는 `EXPIRED`, 코인·정산 없음 |
| CANCELED         | 합의 취소       |
| CANCELED_RELATIONSHIP_ENDED | 연결 종료에 따른 수행 마감 전 강제 취소 |

---

# 10. 파트너 결과 판정과 최종 승인

> 결정 상태: `[확정]` · 출시 범위: `[MVP]`

## 실제 결과 선택

1. `result_at`이 되면 파트너에게 성공 조건과 도전자가 제출한 인증 자료를 확인해 실제 결과를 선택하도록 요청한다.
2. 실제 결과는 파트너만 `SUCCESS` 또는 `FAILURE` 중 하나로 선택한다. 도전자는 결과값을 제출하지 않는다.
3. 선택값은 `PENDING_FINAL_APPROVAL`의 작업 값일 뿐 코인·XP·성장에 반영하지 않는다.
4. 파트너는 최종 승인 전까지 선택값을 바꿀 수 있으며, 변경할 때마다 예상 `row_version`을 검사하고 감사 이벤트를 남긴다.
5. `result_confirmation_deadline_at = result_at + 24시간`이며 실제 결과 선택과 최종 승인 모두 이 시각 전에 완료해야 한다.
6. 결과 확인 시작 직후, 12시간 후, 마감 2시간 전에 파트너에게 거래성 알림과 인앱 대기 항목을 제공한다. 도전자에게는 `파트너 확인 대기` 상태만 보여준다.
7. 시작 승인 때 저장한 `predicted_result`는 수행 중 두 사람에게 표시하지만 실제 결과 선택이나 정산 계산에는 사용하지 않는다.

```text
결과를 확인해줘!

오늘 새벽 1시 전에 자기

실제로 성공했나요?

[성공했어]
[실패했어]
```

선택 뒤에는 별도의 확인 화면을 제공한다.

```text
최종 승인할까요?

등록한 성공 조건
“오전 1시 이전에 잠자리에 들기”

선택한 결과
성공

[결과 수정]
[최종 승인]
[판정 거절]
```

## 최종 승인과 무효 처리

파트너가 최종 승인하면 선택한 결과와 퀘스트·예산·지갑·성장 행을 잠근 뒤 한 트랜잭션에서 정확히 한 번 정산한다. 성공 선택은 `SUCCESS`, 실패 선택은 `FAILURE`로 확정한다. 최종 승인 요청이 재시도되면 새 정산을 만들지 않고 최초 결과를 반환한다.

파트너가 판정 또는 최종 승인을 거절하거나 `result_confirmation_deadline_at`까지 최종 승인이 커밋되지 않으면 `INVALID`로 종료한다. `INVALID`는 성공·실패로 집계하지 않고 잠긴 원금만 반환하며 성공 보너스·XP·인정 성공 수·연승·케미를 바꾸지 않는다. 서버 장애·중복 생성 등 확인 가능한 기술 오류도 `INVALID`를 사용한다.

`invalid_reason`은 최소 `PARTNER_JUDGMENT_REJECTED`, `PARTNER_FINAL_APPROVAL_REJECTED`, `RESULT_CONFIRMATION_TIMEOUT`, `SYSTEM_ERROR`, `RELATIONSHIP_ENDED_BEFORE_FINAL_APPROVAL`로 구분한다. 수행 마감 전 관계 종료는 `INVALID`가 아니라 `CANCELED_RELATIONSHIP_ENDED`로 기록한다.

두 사람이 결과를 따로 입력하거나 서로의 결과를 비교하는 절차는 제공하지 않는다. 따라서 `DISPUTED`, 결과 미응답용 `EXPIRED`, 분쟁 라운드와 24시간 재응답은 일반 개인 퀘스트 상태·API·DB에서 사용하지 않는다.

최근 7일 동안 한 커플의 코인을 건 일반 개인 퀘스트에서 `PARTNER_JUDGMENT_REJECTED`, `PARTNER_FINAL_APPROVAL_REJECTED`, `RESULT_CONFIRMATION_TIMEOUT`에 따른 `INVALID`가 3회 발생하면 손실 회피성 반복 무효를 막기 위해 신규 코인 베팅을 일시 중지한다. `SYSTEM_ERROR`와 관계 종료에 따른 무효는 집계하지 않는다. 두 사람이 정산 규칙을 다시 확인하면 해제하고 이후 사건만 새로 집계한다. 중지 중에도 0C 퀘스트와 연결 종료·차단은 사용할 수 있다.

## 최종 정산표

| 최종 상태 | 잠긴 베팅금 | 성공 보너스·XP | 케미 | 연승 |
| --- | --- | --- | --- | --- |
| `SUCCESS` | 원금 반환 | 지급 | +1 | +1 |
| 파트너 최종 승인 일반 개인 `FAILURE` | 전액 소각 | 없음 | +1 | 0으로 초기화 |
| 일반 개인 `INVALID` | 원금 반환 | 없음 | 없음 | 유지 |
| 수행 마감 전 `CANCELED` | 원금 반환 | 없음 | 없음 | 유지 |
| 튜토리얼 `FAILURE`·`INVALID` | 원금 반환 | 없음 | 없음 | 유지 |
| 마감 전 `CANCELED_RELATIONSHIP_ENDED` | 원금 반환 | 없음 | 없음 | 유지 |
| 마감 후 관계 종료 전 최종 미승인 `INVALID` | 원금 반환 | 없음 | 없음 | 유지 |

---

# 11. 코인 경제 시스템

> 결정 상태: `[확정]` · 출시 범위: `[MVP]`

기본 베팅 방식은 다음과 같다.

파트너가 성공으로 최종 승인하면 잠긴 원금과 시작 승인 시 확정된 난이도 보너스를 받고, 실패로 최종 승인하면 잠긴 원금을 잃는다. 최종 승인을 거절하거나 기한 안에 완료하지 않아 `INVALID`가 되면 원금만 반환한다. 예를 들어 ★★ 퀘스트에 100C를 걸면 성공 보너스는 75C다.

## 기본 정산

### 성공

```text
퀘스트 승인 시 잠금  -100C
걸었던 코인 반환    +100C
성공 보너스          +75C

최종 손익           +75C
```

```text
사용 가능 잔액 1,000C → 900C → 1,075C
```

### 실패

```text
퀘스트 승인 시 잠금  -100C
반환                 0C

최종 손익          -100C
```

```text
사용 가능 잔액 1,000C → 900C
```

## 지갑 상태 전이

| 이벤트 | 사용 가능 잔액 변화 | 잠긴 잔액 변화 |
| --- | ---: | ---: |
| 파트너 시작 승인 | `-stake` | `+stake` |
| 성공 정산 | `+stake + reward` | `-stake` |
| 실패 최종 승인 정산 | 변화 없음 | `-stake` |
| 판정 거절·최종 승인 거절·기한 만료 `INVALID` | `+stake` | `-stake` |
| 수행 마감 전 공동 취소 | `+stake` | `-stake` |
| 서버 기술 오류 무효 | `+stake` | `-stake` |
| 마감 전 연결 종료 강제 취소 | `+stake` | `-stake` |
| 마감 후 관계 종료·최종 미승인 `INVALID` | `+stake` | `-stake` |
| 코인 뽑기 | `-cost` | 변화 없음 |

각 행은 퀘스트 또는 뽑기 상태 변경과 원장 기록을 포함한 하나의 DB 트랜잭션이다.

## 코인 획득처

| 획득 방법 | 보상 | 결정 상태 | 출시 범위 | 우선순위 |
| --- | ---: | --- | --- | --- |
| 최초 가입 활성화 | 계정당 1회 1,000C | 확정 | MVP | P0 |
| 퀘스트 성공 | 난이도별 보너스율과 일일 발행 한도 안에서 순이익 | 확정 | MVP | P0 |
| 구조 퀘스트 | 100C | 확정 | MVP | P0 |
| 일일 출석 | 100C | 확정 | 후속 | P1 |
| 업적 달성 | 100~500C | 제안 | 후속 | 미정 |
| 정식 주말 레이드 | 300~1,000C | 제안 | 후속 | 미정 |
| 시즌 보상 | 등급별 차등 | 제안 | 후속 | 미정 |

## 코인 사용처

| 사용처 | 금액 | 적용 상태 |
| --- | ---: | --- |
| 일반 뽑기 | 100C | `[확정][MVP]` |
| 고급 뽑기 | 500C | `[확정][MVP]` |
| 전설 뽑기 | 1,000C | `[확정][MVP]` |
| 장비 강화 | 미정 | `[제안][후속]` |
| 몬스터 꾸미기 | 미정 | `[제안][후속]` |
| 닉네임 변경권 | 미정 | `[제안][후속]` |
| 커플 방 꾸미기 | 미정 | `[제안][후속]` |

## 베팅 한도

| 난이도 | 선택 가능 금액 | 적용 범위 | 결정 상태 |
| --- | ---: | --- | --- |
| ★ | 0C / 100C | MVP 일반 개인 퀘스트 | 확정 |
| ★★ | 0C / 100C | MVP 일반 개인 퀘스트 | 확정 |
| ★★★ | 0C / 100C / 200C | MVP 일반 개인 퀘스트 | 확정 |
| ★★★★ | 0C / 100C / 200C / 300C | MVP 일반 개인 퀘스트 | 확정 |
| ★★★★★ | 0C / 100C / 300C / 500C | 후속 장기 퀘스트 | 제안 |

MVP 기본 템플릿의 난이도는 서버가 고정하며 사용자가 높일 수 없다. 자유 입력 퀘스트는 표시 난이도와 관계없이 최대 100C만 걸 수 있다. 후속 장기 퀘스트의 1,000C 베팅은 제거하고, 주 1개·주간 성공 보너스 500C를 상한으로 둔다. 500C 장기 퀘스트는 별도 주간 보상 예산과 90일 경제 시뮬레이션을 추가하기 전에는 서버에서 활성화하지 않는다.

## 난이도별 성공 보너스

| 난이도·출처 | 성공 보너스율 | 100C 베팅 예시 | 적용 상태 |
| --- | ---: | ---: | --- |
| ★ 서버 템플릿 | 베팅금의 50% | 50C | `[확정][MVP]` |
| ★★ 서버 템플릿 | 베팅금의 75% | 75C | `[확정][MVP]` |
| ★★★·★★★★ 서버 템플릿 | 베팅금의 100% | 100C | `[확정][MVP]` |
| ★★★★★ 후속 장기 템플릿 | 별도 주간 예산 검증 후 100% | 100C | `[제안][후속]` |
| 자유 입력 퀘스트 | 표시 난이도와 무관하게 50% | 50C | `[확정][MVP]` |

성공 보너스는 25C 단위로 계산한다. 승인 시 서버가 해당 날짜의 전체·저난도 잔여 예산을 적용해 `reward_reserved_amount`를 확정하고 화면에 표시한다. 예산이 일부만 남으면 보너스를 남은 금액으로 낮춰 보여주며, 사용자는 승인하지 않고 0C로 수정할 수 있다. 승인 뒤에는 성공률이나 최종 잔액에 따라 보너스를 바꾸지 않는다.

## 코인 파밍 방지 규칙

1. 동시에 진행 가능한 전체 퀘스트와 하루 XP 지급 퀘스트는 사용자당 최대 5개다.
2. 코인을 건 퀘스트는 `result_at`의 KST 날짜 기준 사용자당 하루 최대 2개다.
3. 일반 개인 퀘스트 성공 보너스는 사용자당 KST 하루 최대 300C다.
4. ★·★★ 서버 템플릿과 자유 입력 퀘스트를 합친 저난도 성공 보너스는 그 300C 안에서 하루 최대 100C다.
5. 승인 트랜잭션에서 해당 결과 예정일의 베팅 슬롯과 계산된 성공 보너스 금액을 예약한다. 전체 300C 또는 저난도 100C를 넘을 수 없다.
6. `FAILURE`·사용자 공동 취소 시 보너스 예약액은 해제하지만 그날 사용한 베팅 퀘스트 횟수는 복구하지 않는다. `INVALID`는 보너스 예약액을 해제하고 사용한 베팅·XP 슬롯을 복구한다.
7. 원금 반환은 신규 발행이 아니므로 300C 한도에 포함하지 않는다.
8. 실패로 코인이 소각되어도 당일 발행 한도를 다시 늘리지 않는다.
9. `INVALID` 원금 반환은 신규 발행이 아니며, 손실 회피성 반복 무효는 10절의 별도 베팅 중지 기준으로 관리한다.

0C 일반 개인 퀘스트도 동시 진행 5개와 `result_at` KST 날짜 기준 XP 슬롯 5개를 사용한다. 튜토리얼·구조 퀘스트는 이 반복 슬롯에서 제외한다. 시작 승인 때 슬롯을 예약하며 `FAILURE`·사용자 취소는 그날 슬롯을 복구하지 않고 `INVALID`만 복구한다.

일일 출석이 도입된 일반 날짜의 반복 보상 총발행 상한은 `퀘스트 성공 보너스 300C + 출석 100C = 400C`다. 구조 퀘스트 사용일은 예외적으로 최대 500C지만 최근 30일 2회만 가능하다. 따라서 30일 반복 보상 총발행 절대 상한은 MVP `9,200C`, 출석 도입 후 `12,200C`다. 최초 가입 1,000C와 튜토리얼 최초 보너스 100C, 후속 업적·정식 레이드·시즌 보상은 반복 보상과 분리된 일회·운영 예산으로 관리한다. 실제 순증감은 이 발행액에서 실패 소각과 뽑기 소비를 뺀 값이다. `INVALID`는 원금 반환이므로 신규 발행·소각 어느 쪽에도 포함하지 않고 별도 관찰한다.

## 30일·90일 코인 수급 시뮬레이션

> 결정 상태: `[제안]` · 출시 범위: `[후속]` P1 출석 도입 후 운영 가정

다음은 일일 출석이 P1으로 도입된 이후의 기준 사용자 가정이다. 일회성 가입·튜토리얼 보너스와 구조 퀘스트, 업적·레이드·시즌 보상은 제외한다.

```text
매일 출석                         +100C
매일 ★★ 100C 퀘스트, 성공률 80%
매일 ★★★ 200C 퀘스트, 성공률 65%
매일 일반 뽑기 1회                -100C
5일마다 고급 뽑기 1회             -500C
시작 잔액                         1,000C
```

```text
일일 기대 성공 보너스
75 × 0.80 + 200 × 0.65 = 190C

일일 기대 실패 소각
100 × 0.20 + 200 × 0.35 = 90C

출석 포함, 뽑기 전 기대 순증가
190 - 90 + 100 = 200C

일일 평균 뽑기 지출
100 + 500 ÷ 5 = 200C

최종 기대 순증가
0C/일
```

| 시점 | 뽑기 미사용 | 계획대로 뽑기 사용 |
| --- | ---: | ---: |
| 시작 | 1,000C | 1,000C |
| 30일 | 7,000C | 1,000C |
| 90일 | 19,000C | 1,000C |

쉬운 퀘스트만 반복하는 최악 전략도 별도로 본다. ★·★★·자유 입력 퀘스트 두 개를 모두 성공해도 저난도 보너스는 하루 100C에서 멈춘다. P1 출석 100C를 받고 계획된 뽑기 200C를 사용하면 순증가는 `100 + 100 - 200 = 0C/일`이다. 뽑기를 전혀 사용하지 않는 보유 전용 사용자는 90일에 19,000C까지 쌓일 수 있으므로 계획된 소비 루프 참여 사용자와 별도 코호트로 관찰하고, 후속 고액 보상이나 유료성 없는 추가 발행을 열기 전에 지속 소비처를 먼저 검증한다.

활동 사용자의 운영 목표는 `성공 보너스·출석 발행액 ÷ 뽑기 소비·실패 소각액 = 0.9~1.1`, 90일 잔액 중앙값 `1,000~3,000C`다. 뽑기를 사용하지 않는 사용자는 코인이 계속 쌓이므로 후속 꾸미기·강화 같은 지속적인 소비처가 필요하다. 출시 후 사용자당 발행액, 실패 소각액, `INVALID` 원금 반환액, 뽑기 소비액, 난이도별 성공률, 30일·90일 잔액 중앙값과 상위 10%를 함께 관찰해 상한을 조정한다.

---

# 12. 코인이 부족할 때

> 결정 상태: `[확정]` · 출시 범위: `[MVP]`

코인이 없어 유료 퀘스트나 뽑기를 할 수 없게 된 사용자를 위해 `구조 퀘스트`를 P0 복구 수단으로 제공한다.

```text
츄코인이 부족해!

구조 퀘스트를 완료하면
100C를 받을 수 있어.

“오늘 물 3잔 마시기”

걸기 없음
보상 100C

[구조 퀘스트 시작]
```

## 구조 퀘스트 규칙

* 사용 가능 잔액이 100C 미만이고 잠긴 코인이 없을 때만 등장
* 서버가 제공하는 안전한 고정 템플릿만 사용
* 코인을 걸지 않고 파트너 확인 없이 서버가 정산
* 7일에 한 번, 최근 30일 최대 2회
* 보상 100C
* 경험치·연승·케미 게이지는 지급하지 않음
* 지급과 원장 기록은 하나의 서버 트랜잭션으로 처리

---

# 13. 경험치와 레벨 시스템

> 결정 상태: `[확정]` · 출시 범위: `[MVP]`

코인은 위험과 보상을 결정하고, 경험치는 퀘스트 난이도로 결정한다. 기존 설정은 별 1개부터 5개까지 각각 10, 20, 40, 70, 150 경험치를 제공한다.

| 난이도   | 기준 예시               | 획득 경험치 |
| ----- | ------------------- | -----: |
| ★     | 간단한 당일 행동           |  10 XP |
| ★★    | 약간의 노력이나 절제가 필요한 행동 |  20 XP |
| ★★★   | 30분 이상 노력해야 하는 목표   |  40 XP |
| ★★★★  | 하루 단위의 어려운 목표       |  70 XP |
| ★★★★★ | 며칠간 이어지는 장기 목표      | 150 XP |

## 난이도 예시

| 퀘스트             | 권장 난이도 |
| --------------- | ------ |
| 물 한 잔 더 마시기     | ★      |
| 오늘 1시 전에 자기     | ★★     |
| 하루 동안 배달 음식 참기  | ★★    |
| 운동 1시간 하기       | ★★★    |
| 공부 3시간 하기       | ★★★★   |
| 일주일 동안 운동 4번 하기 | ★★★★★  |

기본 템플릿의 난이도는 서버가 고정한다. 자유 입력 퀘스트는 도전자가 난이도를 제안하고 파트너가 수정 요청할 수 있지만, 코인 베팅은 최대 100C다.

## 레벨업 필요 경험치

확정 공식은 다음과 같다.

```text
다음 레벨 필요 XP
= 80 + 20 × (현재 레벨 - 1)
```

예를 들면:

| 현재 레벨 | 다음 레벨 필요 XP |
| ----: | ----------: |
|     1 |          80 |
|     5 |         160 |
|    10 |         260 |
|    18 |         420 |
|    20 |         460 |
|    30 |         660 |

경험치는 누적 총합이 아니라 **현재 레벨에서 쌓은 진행 경험치**로 저장하고 화면에는 `현재 레벨 경험치 / 다음 레벨 필요 경험치`로 표시한다. 레벨업할 때 필요 경험치만 차감하며, 초과 경험치는 다음 레벨로 이월한다.

MVP 레벨 상한은 `50`으로 둔다.
`LV.50`에서는 추가 경험치를 쌓지 않고 `nextLevelRequiredExp`를 `null`로 반환하며, 화면에는 `MAX`로 표시한다.

---

# 14. 연속 성공 시스템

연속 성공은 경쟁심을 만들지만 한 번의 실패로 지나치게 큰 손해를 주지 않도록 경험치 보너스만 제공한다.

|   연속 성공 | 효과         |
| ------: | ---------- |
|     3연승 | XP +10%    |
|     5연승 | XP +20%    |
| 10연승 이상 | XP +30%    |
|      실패 | 연승 0으로 초기화 |

연출 예시:

```text
🔥 STREAK ×5

5연속 퀘스트 성공!

기본 경험치    +40
연승 보너스     +8
총 경험치      +48
```

> 결정 상태: `[제안]` · 출시 범위: `[후속]`

칭호도 제공할 수 있다.

```text
3연승  불씨가 붙었츄
5연승  기세 좋츄
10연승 막을 수 없츄
```

---

# 15. 몬스터 성장과 진화

> 결정 상태: `[확정]` · 출시 범위: `[MVP]`

각 플레이어는 MVP에서 한 마리의 활성 배츄를 키운다. **배츄의 외형 성장은 레벨이 아니라 초기화되지 않는 인정 성공 누적 수로 결정한다.**

## 성장 기준 분리

| 시스템 | 증가 기준 | 영향 |
| --- | --- | --- |
| XP·계정 레벨 | 성공 퀘스트의 난이도별 XP | 기본 HP·ATK·전투력 |
| 배츄 인정 성공 수 | 일반 개인 퀘스트 최종 `SUCCESS` | 종별 외형 성장·숙련 보상·새 알 해금 |
| 장비 | 무기·방어구·장신구 장착 | 추가 HP·ATK·전투력 |
| 시즌 | 해당 시즌의 독립 활동 | 한정 꾸미기·칭호·시즌 기록 |

영구 성장 마일스톤은 다음과 같다.

| 조건 | 즉시 처리 | 범위 |
| --- | --- | --- |
| 튜토리얼 성공, 또는 활성 `EGG` 배츄의 첫 인정 성공 | `EGG → BABY` 부화 | `[MVP]`·`[P2]` 공통 |
| 인정 성공 20회 | `BABY → INTERMEDIATE` 중간 성장 | `[MVP]` |
| 인정 성공 40회 | `INTERMEDIATE → FINAL` 최종 진화 | `[MVP]` |
| 인정 성공 60회 | 종별 전용 오라·칭호·능력치 0 숙련 장신구를 각 1개씩 지급 | `[MVP]` |
| 인정 성공 80회 | 선택권 생성 자격 기록, P2 작업자가 미보유 배츄 알 선택권 1개 생성 | `[MVP 달성 기록][후속 사용]` |

## 정확한 성장 시점

성장은 파트너가 실제 결과를 선택한 순간이 아니라 **파트너의 최종 승인을 받고 서버의 최종 정산 트랜잭션이 커밋된 순간** 반영한다.

* 튜토리얼 성공 정산: 인정 성공 수 `0`을 유지한 채 `HATCH` 기록과 `EGG → BABY`를 동시에 처리
* 튜토리얼 미성공 스타터 또는 P2에서 새로 얻은 활성 `EGG` 배츄의 첫 일반 개인 퀘스트 성공 정산: 인정 성공 수 `0 → 1`, `HATCH` 기록, `EGG → BABY`를 동시에 처리
* 인정 성공 `1~19`: `BABY` 유지
* 인정 성공 `19 → 20` 정산: 즉시 `INTERMEDIATE`
* 인정 성공 `20~39`: `INTERMEDIATE` 유지
* 인정 성공 `39 → 40` 정산: 즉시 `FINAL`
* 인정 성공 `59 → 60` 정산: 외형은 `FINAL` 유지, 숙련 오라·칭호·장신구 해금
* 인정 성공 `79 → 80` 정산: 외형은 `FINAL` 유지, `SUCCESS_80` 달성 기록과 outbox 이벤트 생성. P2에서는 커밋 뒤 상시 작업자가 이 이벤트를 소비해 알 선택권을 생성

20·40·60·80을 만든 정산 응답과 홈 갱신 이벤트에 새 단계·보상을 함께 포함한다. 트랜잭션이 실패하면 이전 외형을 유지하고, 같은 정산을 재시도해도 마일스톤과 보상은 중복 지급하지 않는다.

인정 성공 수는 파트너가 최종 승인한 `quest_type = PERSONAL` 일반 개인 퀘스트를 서버가 `SUCCESS`로 정산하는 순간 그 사용자의 활성 배츄에 퀘스트당 1회만 지급한다. 튜토리얼·구조·보스 퀘스트와 `FAILURE`, `CANCELED`, `INVALID`는 제외한다. 튜토리얼을 성공해도 부화만 시키고 인정 성공 수는 0에서 시작한다.

성공 정산 트랜잭션은 정산 시점의 활성 배츄 ID를 `credited_monster_id`로 고정하고 인정 성공 수를 올린다. 이때 활성 배츄가 `EGG`라면 스타터·추가 알 구분 없이 같은 정산에서 `(monster_id, HATCH)`를 한 번만 기록하고 `BABY`로 부화시킨다. 임계값에 도달하거나 데이터 복구로 건너뛰면 누락된 마일스톤을 낮은 순서부터 한 번씩 처리한다. 외형 단계와 인정 성공 수는 시즌 종료·실패·활성 배츄 교체로 감소하지 않는다. 60회 숙련 기록은 달성 배츄에 연결하고, 오라·칭호·장신구는 그 종에 맞는 계정 귀속 보상으로 한 번만 지급해 교환·선물을 금지한다.

## 80회 새 알 선택권

> 결정 상태: `[확정]` · 출시 범위: `[후속]` · 우선순위: `P2`

80회 도달 시 무작위 중복 알을 주지 않고, 사용자가 보유하지 않은 종 중 최대 3종을 중복 없이 제시해 하나를 선택하게 한다. 첫 80회에는 스타팅 4종 중 나머지 3종이 모두 후보가 된다. 후속 달성에서 미보유 종이 1~2개면 남은 전부를 보여주고, 0개면 선택권을 소멸하지 않고 새 종이 추가될 때까지 `PENDING_CONTENT`로 보관한다.

새 배츄는 `EGG`, 인정 성공 0회로 시작하며 활성화한 뒤 첫 인정 성공 정산에서 `BABY`로 부화한다. 기존 배츄는 보관함에 남고, 계정 레벨·XP·기본 능력치·장비·지갑은 그대로 유지한다. 활성 배츄는 한 마리만 허용하며, 교체 순간부터 새로 정산되는 성공만 새 활성 배츄에 귀속된다. 80회 달성 이력은 MVP부터 저장해 후속 기능 출시 시 소급 선택권을 지급한다.

## 기본 능력치

> 결정 상태: `[확정]` · 출시 범위: `[MVP]`

```text
LV.1

HP  100
ATK 10
```

레벨이 오를 때마다:

```text
HP  +8
ATK +2
```

## 전투력 계산

```text
기본 전투력
= 기본 HP × 5 + 기본 ATK × 20

최종 HP
= 기본 HP + 장착 아이템 HP 보너스 합

최종 ATK
= 기본 ATK + 장착 아이템 ATK 보너스 합

아이템 전투력 기여도
= HP 보너스 × 5 + ATK 보너스 × 20

최종 전투력
= 최종 HP × 5 + 최종 ATK × 20
```

전투력의 단일 기준은 최종 HP와 최종 ATK다. 아이템 전투력 기여도는 화면 설명을 위해 위 공식으로 계산하는 파생값이며, 별도 값으로 저장하거나 클라이언트에서 입력하지 않는다. 전투 중 감소한 현재 HP가 아니라 최대 HP를 사용한다.

예시:

```text
계정 LV.18 · 활성 배츄

HP 236
ATK 44

기본 전투력
236 × 5 + 44 × 20
= 2,060

장비 보너스
HP +90
ATK +16

최종 능력치
HP 326
ATK 60

장비 전투력 기여도
90 × 5 + 16 × 20
= 770

최종 전투력
326 × 5 + 60 × 20
= 2,830
```

---

# 16. 뽑기 시스템

> 결정 상태: `[확정]` · 출시 범위: `[MVP]`

뽑기는 100C, 500C, 1,000C 세 종류로 구성한다. 아래 확률은 MVP 확률 버전 `v1`이다.

## 일반 뽑기 — 100C

| 등급     |  확률 |
| ------ | --: |
| COMMON | 75% |
| RARE   | 20% |
| EPIC   |  5% |

주요 보상:

* 기본 무기
* 기본 방어구
* 작은 액세서리
* 포션
* 꾸미기 소품

## 고급 뽑기 — 500C

| 등급        |  확률 |
| --------- | --: |
| RARE      | 50% |
| EPIC      | 40% |
| LEGENDARY | 10% |

주요 보상:

* 불꽃검
* 얼음 갑옷
* 천사 날개
* 왕관
* 특별 몬스터 이펙트

## 전설 뽑기 — 1,000C

| 등급        |  확률 |
| --------- | --: |
| EPIC      | 50% |
| LEGENDARY | 40% |
| MYTHIC    | 10% |

주요 보상:

```text
별을 삼킨 검
MYTHIC
ATK +40
전투력 기여도 +800
```

```text
커플왕의 왕관
LEGENDARY
ATK +15
HP +80
전투력 기여도 +700
```

## 천장 시스템

운이 나쁜 사용자가 계속 손해 보는 것을 막기 위해 다음 보장을 둔다.

| 뽑기    | 보장                             |
| ----- | ------------------------------ |
| 고급 뽑기 | 10회 동안 LEGENDARY 미획득 시 10번째 확정 |
| 전설 뽑기 | 20회 동안 MYTHIC 미획득 시 20번째 확정    |
| 전체 뽑기 | 최근 획득 기록과 현재 천장 횟수 표시          |

모든 확률은 뽑기 화면에 공개한다.

확률과 천장 규칙은 다음과 같이 관리한다.

* 확률표는 뽑기 종류별 불변 버전으로 저장하고, 사용한 버전 ID와 적용 시각을 결과 기록에 남긴다.
* 이미 사용된 확률 버전은 수정하지 않고 변경 시 새 버전을 만든다.
* 고급·전설 천장 카운터는 뽑기 종류별로 독립 관리한다.
* 목표 등급 이상을 획득하면 해당 카운터만 0으로 초기화한다.
* 확률 버전이 바뀌어도 사용자의 기존 천장 진행도는 유지한다.
* 뽑기 전에 전체 확률, 현재 확률 버전과 천장 진행도를 표시한다.

---

# 17. 중복 아이템과 강화

> 결정 상태: 중복 조각·중복 처리 선택 `[확정]`, 강화 `[제안]` · 출시 범위: 자동 조각 `[MVP]`, 보관 선택·강화 `[후속]`

MVP에서 중복 아이템은 자동으로 조각으로 변환한다. 중복 교환이 출시되는 P2부터는 새로 획득한 중복분에 한해 `교환용으로 보관` 또는 `조각으로 변환`을 사용자가 선택하게 한다. P2 출시 전에 이미 변환된 조각을 아이템으로 되돌리지는 않는다.

```text
불꽃검 획득!

이미 보유하고 있는 아이템이야.

불꽃검 조각 ×30으로 변환됐어.
```

## 등급별 조각

| 등급        | 중복 획득 시 |
| --------- | ------: |
| COMMON    |     5조각 |
| RARE      |    15조각 |
| EPIC      |    40조각 |
| LEGENDARY |   100조각 |
| MYTHIC    |   250조각 |

## 강화 예시

```text
불꽃검 +0
ATK +8

필요 재료
불꽃검 조각 30개
100C

[강화하기]
```

```text
불꽃검 +1
ATK +10
```

아이템 강화는 MVP 이후 기능으로 미룬다. 첫 버전에서는 중복 아이템을 `(user_id, item_definition_id)`별 조각 수량으로 저장만 한다. 뽑기 결과 기록과 조각 증가는 같은 트랜잭션으로 처리한다.
MVP에서 유효 능력치 보너스는 아이템 정의의 `hp_bonus`, `attack_bonus`와 같다. 후속 강화 기능을 도입하면 서버가 강화 단계에 따른 유효 HP·ATK 보너스를 계산하고 전투력도 그 유효값으로 계산한다.

## P2 중복 처리 선택

> 결정 상태: `[확정]` · 출시 범위: `[후속]` · 우선순위: `P2`

P2에서 뽑기 결과가 중복이면 추첨 트랜잭션이 결과와 개별 아이템 단위를 `PENDING_DISPOSITION`으로 한 번만 저장한다. 사용자는 24시간 안에 다음 중 하나를 멱등하게 선택한다.

* `KEEP_FOR_EXCHANGE`: 중복 단위를 교환 가능 여분으로 보관
* `CONVERT_TO_FRAGMENTS`: 등급별 조각으로 영구 변환

선택 전에는 장착·강화·교환·선물할 수 없고, 24시간 만료 시 서버가 `CONVERT_TO_FRAGMENTS`로 자동 처리한다. 선택 재시도는 새 아이템이나 조각을 추가로 만들지 않으며, 추첨 결과는 선택 재시도로 다시 생성하지 않는다.

---

# 18. 아이템과 장비

> 결정 상태: `[확정]` · 출시 범위: `[MVP]`

장비 슬롯은 세 개만 사용한다.

| 슬롯  | 주요 능력치             |
| --- | ------------------ |
| 무기  | 공격력 증가             |
| 방어구 | 체력 증가              |
| 장신구 | 공격력·체력 혼합 또는 특수 효과 |

## 능력치 장비의 전투력 기여도 범위

| 등급        | 파생 전투력 기여도 범위 |
| --------- | ----------: |
| COMMON    |      20~100 |
| RARE      |     105~250 |
| EPIC      |     255~500 |
| LEGENDARY |     505~750 |
| MYTHIC    |   755~1,000 |

등급별 범위는 강화하지 않은 `+0` 능력치 장비의 HP·ATK 보너스로부터 계산한 기여도의 밸런스 기준이다. 포션·꾸미기·이펙트처럼 능력치가 없는 아이템은 이 범위에서 제외하고 전투력 기여도를 `0`으로 처리한다.

## 몬스터 화면 예시

```text
계정 LV.18 · 사용자 1

        🐲

기본 능력치
HP 236 / ATK 44

[무기]
별빛검
ATK +12

[방어구]
구름 갑옷
HP +70

[장신구]
사랑의 반지
ATK +4 / HP +20

장비 보너스
HP +90 / ATK +16

최종 능력치
HP 326 / ATK 60

종합 전투력
2,830
```

MVP에서는 사용자 간 아이템 교환·선물을 지원하지 않는다. 후속 기능도 현재 연결된 커플 내에서만 허용하고, 아이템 몰아주기·반복 계정·상태 경합을 막는 제한을 먼저 구현한다. 코인·뽑기권·조각의 사용자 간 송금·교환·선물은 계속 금지한다.

## 후속 출시 순서

> 결정 상태: `[확정]` · 출시 범위: `[후속]` · 우선순위: `P2`

| 단계 | 기능 | 이유 |
| --- | --- | --- |
| P2-1 | 상점에서 상대에게 사 주는 전투력 0 귀속 꾸미기 선물 | 기존 장비 이전 없이 선물 상호작용 먼저 검증 |
| P2-2 | 중복 아이템 1:1 교환 | 등급·횟수·연결 기간 제한으로 경제 안전성 검증 |
| P2-3 | 기존 보유 꾸미기·저등급 중복 아이템 선물 | 개별 아이템 귀속·반환·재이전 금지 검증 후 출시 |

## 커플 아이템 1:1 교환

> 결정 상태: `[확정]` · 출시 범위: `[후속]` · 우선순위: `P2-2`

* 현재 `CONNECTED`인 커플이고 `connected_at + 7일`이 지난 후에만 사용
* 각자가 동일 아이템 1개를 남긴 뒤의 **중복 여분 1개**씩만 1:1로 제안
* `COMMON`~`EPIC`만 허용하고 두 아이템의 등급이 같거나 서버 `exchange_value_group`이 같아야 함
* 장착·강화·한정·귀속·선물받은 아이템과 `LEGENDARY`·`MYTHIC`은 제외
* 코인·뽑기권·조각은 제안 항목으로 사용 불가
* 제안이 바뀌면 `offer_version + 1`하고 양쪽 확인을 모두 초기화
* 양쪽이 같은 `offer_version`을 최종 확인한 때만 원자적으로 소유권 이전
* 커플 기준 완료 주 3회로 제한하고, 제안은 생성 24시간 후 만료

최초 제안자는 자기 중복 여분만 선택·예약하고 원하는 상대 아이템 정의를 정확히 하나 요청한다. 파트너 소유 단위를 대신 예약할 수 없으며, 파트너가 넣는 단위의 `item_definition_id`는 요청한 정의와 같아야 한다. 각 사용자가 자기 단위를 추가하거나 교체하면 바꾸지 않은 상대 단위는 새 버전에 그대로 복사하고, 본인의 이전 단위 예약은 해제한 뒤 새 단위를 예약하는 작업을 한 트랜잭션으로 처리한다. 그 뒤 `offer_version`을 올리고 양쪽 확인을 초기화한다. 양쪽 단위가 모두 있는 현재 버전만 최종 확인할 수 있으며, 예약 후에도 각 사용자가 해당 정의 아이템 1개를 남겨야 한다. 완료 트랜잭션은 커플·주간 예약+완료 한도·제안·개별 아이템을 잠그고 연결 상태·7일·소유권·가용 수량·등급·버전·주 3회 한도를 다시 검사한다. 완료하면 두 ACTIVE 예약을 `CONSUMED`로 바꾸고 같은 두 개별 단위의 `owner_id`를 맞바꾼 뒤 `state = AVAILABLE`, `binding_type = UNBOUND`로 되돌리며 이전 이력을 추가한다.

교환 슬롯은 생성 시점의 KST 주간 예산에 먼저 예약한다. 완료 시점이 다음 주라면 기존 주의 예약을 줄이고 완료 주 예산으로 원자 이동한 뒤 `reserved → completed`로 바꾼다. 완료 주의 주 3회 한도가 이미 찼으면 마지막 확인과 완료를 커밋하지 않고 `409 WEEKLY_EXCHANGE_LIMIT_REACHED`를 반환하며, 사용자가 24시간 만료 전 다시 확인할 수 있게 한다. 자동 완료 대기 상태는 만들지 않는다. 연결 종료·차단·취소·만료 시 미완료 제안을 종료하고 현재 예약 단위와 주간 예약 수를 즉시 해제한다.

## 아이템 선물

> 결정 상태: `[확정]` · 출시 범위: `[후속]` · 우선순위: `P2-1`, `P2-3`

P2-1에서는 상점의 불변 `shop_offering_version`에 등록된 전투력 0 꾸미기만 상대에게 사 줄 수 있다. 보내는 사람의 코인은 수락 전까지 잠그고, 수락 시 가격 스냅샷대로 차감하며 `state = AVAILABLE`, `binding_type = TRANSFER_LOCKED`인 수령자 귀속 꾸미기 단위를 생성한다. 생성 뒤 상점 버전이 종료돼도 이미 대기 중인 선물은 가격 스냅샷대로 수락할 수 있다. 거절·72시간 만료·연결 종료·차단 시 잠긴 코인을 전액 반환한다.

`gift_duplicate_policy = REJECT_IF_OWNED`인 아이템은 상점·보유 아이템 선물 모두 수령자가 같은 정의를 이미 보유하면 생성할 수 없다. 생성 뒤 다른 경로로 먼저 획득했다면 수락을 거절하고 코인 또는 예약 단위를 반환한다. 이 정책은 선물에만 적용하므로 뽑기 중복 보관과 교환용 여분 보유를 막지 않는다.

P2-3의 기존 보유 아이템 선물은 다음 제한을 적용한다.

* 전투력 0 꾸미기 또는 `COMMON`·`RARE` 중복 여분만 허용
* 장착·강화·한정·귀속 아이템과 코인·뽑기권·조각은 선물 불가
* 생성 시 해당 개별 단위를 예약하고 수령자가 수락한 때만 소유권 이전
* 거절·72시간 만료·연결 종료·차단 시 보낸 사람에게 예약 단위 반환
* 선물받은 개별 단위는 영구 `TRANSFER_LOCKED`로 귀속해 재선물·재교환 불가
* `TRANSFER_LOCKED` 단위는 조각 변환도 불가
* 보내는 사람 기준 완료 주 1회로 제한하고 병렬 예약으로 우회할 수 없게 주간 한도를 잠금
* 수령자는 설정에서 선물 수신을 끄고, 이 경우 새 선물 요청을 생성할 수 없음

보유 아이템 선물을 수락하면 ACTIVE 예약을 `CONSUMED`로 바꾸고 같은 개별 단위의 `owner_id`를 수령자로 바꾼 뒤 `state = AVAILABLE`, `binding_type = TRANSFER_LOCKED`로 저장해 수령자는 장착할 수 있지만 재이전할 수 없게 한다. 생성 주에 주간 슬롯을 예약하고 완료 주가 달라지면 교환과 같은 방식으로 완료 주 예산으로 옮긴 뒤 보내는 사람 기준 주 1회를 확정한다. 완료 주 한도가 찼으면 수락을 커밋하지 않고 `409 WEEKLY_GIFT_LIMIT_REACHED`를 반환한다.

수령자가 선물 수신을 끄면 새 요청을 즉시 막고 기존 `PENDING` 선물은 더 이상 수락할 수 없게 한다. 설정 변경 트랜잭션이 대상 선물 스냅샷과 `gift_settings_cleanup_job`을 만들고, 작업자가 각 선물을 `REJECTED_SETTINGS_DISABLED`로 종료해 상점 선물 코인과 보유 아이템 예약을 멱등 반환한다. 설정 API는 작업 ID와 대상·완료 수를 반환한다. 다시 켜도 스냅샷된 정리 작업은 끝까지 수행하고 종료된 선물은 되살리지 않는다.

## 꾸미기 적용

> 결정 상태: `[확정]` · 출시 범위: `[후속]` · 우선순위: `P2-1`

P2-1의 선물 가능 꾸미기는 `HEAD`, `BODY`, `BACKGROUND` 중 한 슬롯을 가지며 전투력은 항상 0이다. 사용자는 보유한 꾸미기 단위를 슬롯별로 하나씩 적용·해제할 수 있다. 적용 중인 단위도 `TRANSFER_LOCKED` 귀속은 유지하고, 외형 표시 외에 HP·ATK·전투력에는 영향을 주지 않는다.

---

# 19. PvP 배틀

> 결정 상태: `[제안]` · 출시 범위: `[후속]`

PvP는 단순 전투력 비교가 아니라 선택형 3턴 전투로 만든다. 기존 기획에서도 공격·방어·스킬 중 하나를 동시에 선택하는 방식이 제안됐다.

## 기본 규칙

각 턴마다 두 사람이 하나를 고른다.

```text
⚔️ 공격
🛡️ 방어
✨ 스킬
```

상대방이 무엇을 선택했는지 모르는 상태에서 동시에 공개한다.

```text
공격 > 스킬
스킬 > 방어
방어 > 공격
```

## 선택 결과 보정

| 결과    | 피해량 배율 |
| ----- | -----: |
| 상성 승리 |   ×1.3 |
| 같은 선택 |   ×1.0 |
| 상성 패배 |   ×0.7 |

몬스터와 장비의 능력치가 기본 피해량을 결정하지만, 선택을 잘하면 전투력이 낮은 사람도 이길 수 있게 만든다.

## 승리 조건

* 총 3턴 진행
* 3턴 후 남은 HP가 많은 플레이어 승리
* HP가 같으면 총 피해량이 높은 플레이어 승리
* 모두 같으면 무승부

## PvP 보상

PvP에서 큰 코인을 지급하면 강한 사람이 계속 더 강해질 수 있다. 따라서 보상 차이를 작게 둔다.

| 결과  |             보상 |
| --- | -------------: |
| 승리  | 50C + 배틀 메달 3개 |
| 패배  | 20C + 배틀 메달 1개 |
| 무승부 | 30C + 배틀 메달 2개 |

배틀 메달은 능력치 장비보다 프로필 테두리, 승리 포즈, 칭호 등에 사용하는 것이 좋다.

---

# 20. 주말 협동 콘텐츠

> 결정 상태: 간이 협동 보스 `[확정]`, 정식 레이드 `[제안]` · 출시 범위: 간이 협동 보스 `[MVP]`, 정식 레이드 `[후속]`

커플 게임이 PvP만 제공하면 한 명이 계속 패배해 흥미를 잃을 수 있다. 그래서 평일에는 경쟁하고 주말에는 함께 보스를 상대하는 구조를 사용한다.

## 보스전 해금 조건

> 결정 상태: `[확정]` · 출시 범위: `[MVP]`

유효한 일반 개인 퀘스트가 파트너의 최종 승인으로 `SUCCESS` 또는 `FAILURE`로 정산될 때마다 `케미 게이지`가 1칸 오른다.

```text
이번 주 케미 게이지

████████░░
8 / 10
```

10칸을 채우면 주말 보스가 해금된다.

게이지 집계 규칙은 다음과 같다.

* 승인 후 실제 시작된 일반 개인 퀘스트만 집계한다.
* 파트너가 최종 승인한 `SUCCESS`와 `FAILURE`는 퀘스트별 1회 집계한다.
* `REJECTED`, `CANCELED`, `APPROVAL_EXPIRED`, `INVALID`, 튜토리얼·구조 퀘스트는 제외한다.
* 정산 완료 시각을 기준으로 매주 월요일 00:00부터 일요일 23:59까지 KST로 집계한다.
* MVP 간이 협동 보스는 토요일 00:00부터 일요일 23:59까지 KST에 플레이한다. 주말 전에 10개를 채우면 토요일에 해금되고, 주말 중 10개에 도달하면 즉시 해금된다.

## MVP 간이 협동 보스

> 결정 상태: `[확정]` · 출시 범위: `[MVP]`

MVP에서는 공격·방어·스킬 턴을 만들지 않고, 기존 케미 게이지와 전투력을 그대로 사용해 경쟁·협동 가치를 검증한다.

1. 두 사람의 스타팅 배츄가 모두 있는 커플은 월요일 00:00 KST에 두 몬스터의 장비 제외 기본 전투력 합을 기록한다.
2. 보스 요구 전투력은 `기본 전투력 합 × 1.2`를 100단위로 올림한 값이다. 주중 새로 연결될 때 두 배츄가 이미 있으면 연결 완료 시점에 계산하고, 한쪽이라도 없으면 두 스타팅 배츄가 모두 생성된 시점에 계산한다.
3. 케미 게이지 10칸을 채운 뒤 주말에 두 사람이 각각 `참여`를 누른다. 첫 번째 참여는 동의 시각만 저장하고 상태를 `WAITING_PARTNER`로 바꾼다.
4. 두 번째 참여 요청 트랜잭션에서 두 사람의 그 시점 최종 전투력을 동시에 계산해 한 번만 스냅샷으로 저장한다. 첫 번째 참여 이후 바뀐 장비도 이 시점 값에 반영한다.
5. 합산 전투력이 요구 전투력 이상이면 클리어한다.
6. 커플당 주 1회만 판정하며 **클리어한 경우에만** 두 사람에게 동일하게 일반 뽑기권 1장을 지급한다. 코인은 발행하지 않는다.
7. 실패해도 코인·아이템을 잃지 않으며 참여 기록만 남긴다.
8. 일요일 23:59까지 한 명만 참여했거나 아무도 참여하지 않으면 `EXPIRED`로 종료한다. 보상·손실은 없고 다음 주로 이월하지 않는다.
9. 판정 전에 연결이 종료되면 `CANCELED_RELATIONSHIP_ENDED`로 종료하고 보상을 지급하지 않는다.

상태는 `LOCKED`, `AVAILABLE`, `WAITING_PARTNER`, `CLEARED`, `FAILED`, `EXPIRED`, `CANCELED_RELATIONSHIP_ENDED`로 관리한다.

```text
MVP CO-OP BOSS

요구 전투력 5,200

사용자 1 2,830
사용자 2 3,120
합산     5,950

CLEAR
두 사람 모두 일반 뽑기권 ×1
```

홈에는 두 사람의 현재 전투력·연승·이번 주 성장량을 나란히 보여주는 무보상 비교 카드를 둔다. `이번 주 성장량 = 현재 최종 전투력 - 해당 커플의 주간 기준 전투력`이다. 기존 커플은 월요일 00:00, 배츄를 이미 가진 사용자가 주중 새 커플로 연결되면 연결 완료 시점, 기준값이 없는 신규 사용자는 스타팅 배츄 생성 시점 값을 사용한다. 비교 카드와 간이 보스로 경쟁과 협동이 실제 퀘스트 재참여를 만드는지 측정한다.

## 선택형 정식 레이드

> 결정 상태: `[제안]` · 출시 범위: `[후속]`

정식 레이드에서는 다음과 같이 보스 HP와 턴 선택을 추가한다.

## 정식 레이드 보스 순환 구성

> 결정 상태: `[확정]` · 출시 범위: `[후속]` · 우선순위: `P2`

정식 레이드를 출시할 때는 잠꾸러기 마왕 하나만 반복하지 않고 **일반 보스 3종 + 시즌 한정 보스 1종**을 4주 단위로 순환한다.

| 주차 | 보스 | 유형 | 핵심 테마 |
| --- | --- | --- | --- |
| 1주차 | 잠꾸러기 마왕 | 일반 | 수면·느긋잠 |
| 2주차 | 미루기 슬라임 | 일반 | 미루기·실천 |
| 3주차 | 산만이 도깨비 | 일반 | 집중·산만함 |
| 4주차 | 해당 시즌 한정 보스 | 시즌 | 운영 시즌 테마 |

전투 엔진과 턴 규칙은 공통으로 재사용하고, 보스 외형·스킬 세트·보상표·연출만 불변 버전 데이터로 교체한다. 매주 월요일 00:00 KST에 주간 레이드 인스턴스를 만들며 적용할 `boss_definition_version_id`를 스냅샷한다. 주중에 운영 데이터가 바뀌어도 이미 시작한 레이드의 보스·난이도·보상은 바뀌지 않는다.

```text
WEEKLY RAID · 1주차 예시

잠꾸러기 마왕

HP 32,000

사용자 1 전투력
2,830

사용자 2 전투력
3,120

커플 합산 전투력
5,950

[레이드 시작]
```

## 전투 방식

> 결정 상태: `[제안]` · 출시 범위: `[후속]`

두 플레이어가 각자 공격·방어·스킬을 선택한다. 보스도 턴마다 행동한다.

커플이 같은 선택을 하면 합동 효과가 발생한다.

```text
두 사람 모두 스킬 선택!

COUPLE ATTACK

5,950 DAMAGE
```

## 보상

> 결정 상태: `[제안]` · 출시 범위: `[후속]`

| 보스 난이도 | 보상               |
| ------ | ---------------- |
| 일반     | 300C + 고급 뽑기권    |
| 어려움    | 500C + 고급 뽑기권 2장 |
| 악몽     | 1,000C + 전설 뽑기권  |
| 시즌 보스  | 한정 장비 또는 몬스터 스킨  |

보스전 보상은 두 사람에게 동일하게 지급한다.

---

# 21. 월간 시즌

> 결정 상태: `[제안]` · 출시 범위: `[후속]`

한 달을 하나의 시즌으로 관리한다.

```text
SEASON 1

8월 시즌

종료까지
2일 18시간
```

## 시즌 기록

```text
8월 결산

퀘스트 성공률

사용자 1 78%
사용자 2 71%

몬스터 전투력

사용자 1 4,820
사용자 2 5,105

최고 연승
사용자 1 11회

최다 획득 코인
사용자 2 8,240C

함께 완료한 퀘스트
42개

클리어한 커플 레이드
4회
```

## 시즌 보상 방향

개인 순위를 강조하기보다 두 사람이 함께 받은 기록과 보상을 충분히 제공한다.

| 조건            | 보상         |
| ------------- | ---------- |
| 해당 시즌 내 개인 퀘스트 20개 성공 | 시즌 개인 칭호 |
| 커플 합산 30개 성공  | 커플 프로필 테두리 |
| 주말 레이드 4회 참여  | 한정 장신구     |
| 한 달간 7일 이상 접속 | 시즌 몬스터 배경  |

시즌 성공 집계는 시즌마다 초기화하는 별도 기록이며, 15절의 영구 배츄 인정 성공 수와 외형 성장은 초기화하지 않는다. 시즌이 끝나도 계정 레벨과 아이템은 유지한다. 전투력이 지나치게 차이 나는 문제는 PvP 능력치 보정으로 해결한다.

---

# 22. 홈 화면 구성

> 결정 상태: `[확정]` · 출시 범위: `[MVP]`

하단 메뉴는 다섯 개로 구성한다.

```text
[홈] [퀘스트] [몬스터] [상점] [배틀]
```

## 홈 화면

```text
BETCHU                         1,840C

사용자 1의 배츄
[사용자 지정 배츄 이름]
계정 LV.18

             🐲

EXP
████████░░
344 / 420 XP

전투력 2,830
연승 4

오늘의 퀘스트

🌙 1시 전에 자기
100C · 결과 대기

🏋️ 운동 1시간
200C · 진행 중

사용자 2의 배츄

LV.20
전투력 3,120

“나보다 290 높아 😳”
이번 주 성장 사용자 1 +350 / 사용자 2 +280

이번 주 케미
8 / 10

처리할 항목 1
[실제 결과 확인]

[새 퀘스트 만들기]
```

홈의 우선순위는 다음과 같다.

1. 내 몬스터 상태
2. 현재 코인
3. 처리해야 할 퀘스트
4. 파트너와의 비교
5. 케미 게이지와 MVP 간이 협동 보스 진행도

---

# 23. 전체 화면 목록

> 결정 상태: `[확정]` · 출시 범위: `[MVP]`

| 화면        | 주요 기능                  |
| --------- | ---------------------- |
| 스플래시      | 로고와 서비스 문구             |
| 로그인       | 소셜 로그인                 |
| 커플 연결     | 초대 코드 생성·미리보기·확정·철회   |
| 스타팅 배츄 선택 | 몬스터 외형 선택              |
| 홈         | 몬스터, 퀘스트, 파트너 비교, 처리 대기함 |
| 인앱 대기함   | 시작 승인·실제 결과·최종 승인·기한 처리 항목 |
| 퀘스트 목록    | 승인 대기·진행 중·결과 대기·완료    |
| 퀘스트 생성    | 제목, 조건, 난이도, 코인, 마감 입력 |
| 퀘스트 시작 승인 | 성공·실패 예상 선택, 승인·수정 요청·거절 |
| 파트너 결과 확인 | 성공 조건·인증 자료 확인, 실제 성공·실패 선택 |
| 결과 최종 승인 | 선택 결과 수정·최종 승인·판정 거절·남은 기한 |
| 정산 결과     | 코인·경험치·연승 변화           |
| 구조 퀘스트    | 잔액 고갈 복구용 시스템 퀘스트      |
| 몬스터       | 계정 레벨·인정 성공 수·외형 성장·숙련 보상 |
| 인벤토리      | 획득 아이템 목록              |
| 장비        | 무기·방어구·장신구 장착          |
| 상점        | 일반·고급·전설 뽑기            |
| 뽑기 결과     | 아이템 연출과 중복 처리          |
| 간이 협동 보스  | 케미 10칸, 참여, 합산 전투력 판정   |
| 설정        | 알림 미리보기, 연결 종료, 차단, 신고, 내보내기, 탈퇴 |

## 후속 화면

> 결정 상태: 표의 기능별 본문 상태를 따름 · 출시 범위: `[후속]`

| 화면 | 주요 기능 |
| --- | --- |
| PvP | 3턴 커플 배틀 |
| 정식 레이드 | 4주 순환 보스와 선택형 주말 보스전 |
| 시즌 | 월간 통계와 보상 |
| 배츄 보관함 | 복수 배츄, 80회 알 선택, 활성 배츄 교체 |
| 중복 처리 | 교환용 보관 또는 조각 변환 선택 |
| 아이템 교환 | 현재 커플 중복 여분 1:1 교환 |
| 꾸미기 상점·선물함 | 귀속 꾸미기 구매 선물과 수락·거절·만료 |

---

# 24. 대표 사용자 시나리오

> 결정 상태: `[확정]` · 출시 범위: `[MVP]`

## 퀘스트 등록

사용자 1이 퀘스트를 만든다.

```text
오늘 새벽 1시 전에 자기

난이도 ★★
걸기 100C
결과 확인 내일 오전 9시 ~ 모레 오전 9시
```

## 파트너 시작 승인

사용자 2가 조건을 확인한다.

```text
“1시 전에 자기면 별 2개 적당하네. 성공할 것 같아.”

[성공 예상]
[시작 승인]
```

파트너의 성공 예상이 두 사람 화면에 표시된다. 사용자 1의 100C가 잠기고 ★★ 보너스 예산 75C가 예약된다.

## 퀘스트 성공

다음 날 사용자 2가 성공 조건과 선택 인증 자료를 확인해 `SUCCESS`를 선택하고 최종 승인한다.

```text
QUEST CLEAR!

걸었던 코인 반환 +100C
성공 보너스       +75C
경험치          +20 XP
연승            3 → 4
케미 게이지      +1
```

사용 가능 잔액은 승인 시 1,000C에서 900C가 되고, 성공 정산 후 1,075C가 된다.

## 뽑기

사용자 1은 500C 고급 뽑기를 진행한다.

```text
EPIC!

구름 갑옷

HP +70
전투력 기여도 +350
```

장비를 장착해 전투력이 상승한다.

```text
2,480 → 2,830
```

## MVP 간이 협동 보스 시나리오

그 주에 파트너 최종 승인으로 일반 개인 퀘스트를 `SUCCESS` 또는 `FAILURE`로 유효하게 10개 정산하면 간이 협동 보스가 열린다.

주말에 첫 사용자가 참여하면 `WAITING_PARTNER`가 된다. 두 번째 사용자가 참여할 때 서버가 두 사람의 현재 전투력 `2,830 + 3,120 = 5,950`을 동시에 저장한다. 요구 전투력 5,200을 넘겨 클리어하고 두 사람 모두 일반 뽑기권 1장을 받는다.

이 한 사이클이 BETCHU의 대표적인 사용자 경험이다.

---

# 25. 알림 문구

> 결정 상태: `[확정]` · 출시 범위: 거래성 알림 `[MVP]`, 선택형 알림 `[후속]`

BETCHU의 알림은 관리 앱처럼 딱딱하지 않고, 몬스터가 직접 말하는 느낌으로 만든다.

커플 연결 최종 확인·만료 임박, 퀘스트 시작 승인·수정 요청·공동 취소 요청·동의·거절·파트너 실제 결과 선택·최종 승인·마감, 간이 보스 해금·파트너 참여 대기·주말 마감 알림과 인앱 대기함은 거래성 P0 기능이다. 푸시를 거부해도 홈 배지와 인앱 대기함에서 같은 항목과 남은 시간을 확인할 수 있어야 한다. 출석·이벤트·홍보 알림은 P1이며 기본 수신 동의를 강제하지 않는다.

P2 기능 출시 뒤에는 중복 처리 선택 마감, 교환 제안·상대 아이템 추가·최종 확인·만료, 선물 수락·거절·만료도 해당 기능의 필수 거래성 알림으로 추가한다. 수령자가 선물 수신을 끈 경우에는 새 선물 알림도 만들지 않는다.

잠금 화면 미리보기는 기본적으로 파트너 이름, 퀘스트 제목, 인증 자료, 판정 결과와 코인 금액을 숨긴다.

> BETCHU에서 확인할 내용이 있어요.
>
> 앱을 열어 확인해 주세요.

아래 상세 문구는 앱 내부 또는 사용자가 `알림 내용 미리보기`를 켠 경우에만 사용한다.

## 퀘스트 도착

> 사용자 1이 100C를 걸었어!
> **“오늘 1시 전에 자기”**를 확인해줘.

## 승인 완료

> 사용자 2가 성공을 예상하고 퀘스트 시작을 승인했어.
> 이제 진짜 지켜야 해!

## 마감 임박

> 약속 종료까지 1시간!
> 배츄가 긴장하고 있어.

## 결과 확인

> 파트너의 실제 결과 확인이 필요해.
> 성공 조건과 인증 자료를 확인해줘!

## 최종 승인 마감

> 결과 최종 승인 마감까지 2시간 남았어.
> 기한이 지나면 퀘스트가 무효 처리되고 원금만 반환돼.

## 최종 승인 대기

> 선택한 실제 결과를 한 번 더 확인해줘.
> 최종 승인 뒤에 정산이 시작돼.

## 무효 처리

> 결과가 최종 승인되지 않아 퀘스트가 무효 처리됐어.
> 걸어둔 원금은 반환됐어.

## 성공

> 퀘스트 성공!
> 배츄가 20XP를 먹고 신나게 뛰어다녀.

## 배츄 성장

> 20번째 인정 성공 정산 완료!
> 배츄가 중간 모습으로 성장했어.

## 실패

> 이번엔 실패했어.
> 배츄가 조금 시무룩하지만 다음 퀘스트는 기다리고 있어.

## 간이 협동 보스 해금

> 케미 게이지가 가득 찼어!
> 둘이 참여하고 합산 전투력을 확인할 시간이야.

## 간이 협동 보스 파트너 대기

> 파트너가 먼저 준비했어!
> 네가 참여하면 이번 주 합산 전투력이 확정돼.

## 간이 협동 보스 마감

> 이번 주 협동 보스 참여가 곧 끝나.
> 일요일 23:59 전에 확인해줘.

---

# 26. 실패 연출 원칙

> 결정 상태: `[확정]` · 출시 범위: `[공통]`

실패는 게임의 일부지만, 사용자를 비난하면 실제 커플 싸움으로 이어질 수 있다.

따라서 실패 문구는 놀리더라도 공격적이지 않게 만든다.

### 취침 퀘스트 실패

> 배츄에게 다크서클이 생겼어…
> 오늘은 조금 더 일찍 재워주자.

### 운동 퀘스트 실패

> 운동화는 준비됐는데 주인은 나타나지 않았어.

### 게임 종료 퀘스트 실패

> 게임의 유혹이 너무 강했다!
> 다음에는 배츄가 종료 버튼을 지켜볼게.

다음 문구는 피한다.

* “역시 못 지켰네.”
* “신뢰도가 떨어졌습니다.”
* “상대방에게 실망을 줬습니다.”
* “거짓말쟁이입니다.”

몬스터는 약속을 평가하는 심판이 아니라, 실패해도 다시 도전하도록 돕는 캐릭터여야 한다.

---

# 27. 안전하고 건강한 퀘스트 규칙

> 결정 상태: `[확정]` · 출시 범위: `[공통]`

## 27.1 퀘스트·사용자 안전

기본 템플릿에서는 다음과 같은 퀘스트를 제공하지 않는다.

* 몸무게나 외모를 평가하는 약속
* 식사를 무조건 거르는 약속
* 사생활 공개를 강요하는 약속
* 위치 인증을 강요하는 약속
* 성적인 행동을 조건으로 거는 약속
* 헤어짐이나 질투를 소재로 한 약속
* 상대방에게 돈을 쓰도록 강요하는 약속
* 위험한 행동이나 과도한 운동
* 연락을 강제로 통제하는 약속

커스텀 퀘스트는 반드시 상대방이 거절하거나 수정 요청할 수 있어야 한다.

커스텀 퀘스트, 상대 사용자와 인증 자료 화면에는 항상 신고·차단 진입점을 제공한다. 위험 행위, 성적 행위, 자해, 식사 제한, 위치 공개, 금전 지출, 연락 빈도·답장 시간 강요는 신고 대상이며, 상대방은 불이익 없이 거절할 수 있다. 신고된 콘텐츠는 검토 중 상대방 화면에서 임시 비노출 처리할 수 있다.

가입과 상점에는 츄코인이 현금 구매·환전·양도·현물 보상이 없는 가상 재화임을 명확히 표시한다. 뽑기 전에는 확률, 천장 진행도와 사용되는 확률 버전을 공개한다.

## 27.2 인증 자료 개인정보 수명주기

인증 이미지는 필수가 아닌 선택 사항이다.

* 퀘스트당 최대 3장, 장당 최대 10MB, 총 20MB
* JPEG·PNG·WebP만 허용하고 서버가 실제 파일 형식·크기·악성 파일 여부를 검사
* 업로드 후 서버에서 재인코딩해 EXIF·GPS 등 메타데이터를 제거하고 변환 성공 즉시 원본 삭제
* 비공개 객체 저장소의 `object_key`만 저장하고 영구 공개 URL은 생성하지 않음
* 업로드 URL 발급 시 `quest_id`에 연결하고, 업로드 완료 호출 후 형식·악성 파일 검사와 재인코딩 수행
* 검사 완료 상태가 `READY`인 처리본만 백엔드 미디어 게이트웨이가 요청마다 현재 커플 연결과 퀘스트 권한을 다시 확인해 스트리밍
* 객체 저장소 직접 조회 URL이나 재사용 가능한 서명 URL은 클라이언트에 반환하지 않고 응답에 `Cache-Control: no-store` 적용
* 제출자, 업로드 시각, 크기, 체크섬, 처리·접근·삭제 이력 기록
* 퀘스트 최종 정산 30일 후 자동 삭제
* 제출자가 삭제하거나 선택 인증 자료 동의를 철회하면 모든 사용자 접근을 즉시 차단하고 일반 저장본을 7일 이내 물리 삭제
* 연결 종료 시 상대방 접근은 즉시 차단하고, 제출자 내보내기 기한은 `min(최종 정산 + 30일, 연결 종료 + 30일)`로 설정해 원래 보관 기간을 연장하지 않음
* 제출자는 삭제 전 내 데이터 내보내기에서 자신이 올린 처리본만 내려받을 수 있음
* 안전 신고에 필요한 자료만 일반 저장본과 분리된 제한 저장소로 격리하고 `hold_until`까지 담당자에게만 허용하며 최대 180일 후 삭제

## 27.3 연령·동의

국내 MVP는 만 14세 이상만 허용한다. 만 14세 미만 개인정보 처리에는 법정대리인 동의 확인이 필요한 점을 고려해 MVP에서는 가입을 받지 않는다. 개인정보 처리방침·이용약관·선택 인증 자료 동의를 구분하고 버전과 동의 시각을 보관하며, 철회와 탈퇴 경로를 설정에서 항상 제공한다. 선택 인증 자료 동의를 철회하면 신규 업로드를 즉시 막고 기존 자료는 27.2절 삭제 절차로 보낸다. 서비스 제공에 필수인 약관·개인정보 처리 동의를 철회하면 데이터 내보내기 안내 후 계정 탈퇴 흐름으로 연결한다. 실제 출시 전에는 대상 국가와 앱 마켓 기준을 포함해 법률 검토를 다시 진행한다.

## 27.4 접근성 출시 기준

* 모든 버튼·입력·상태·오류에 스크린리더용 이름, 역할과 안내 제공
* 논리적인 포커스 순서와 키보드·스위치 탐색 지원
* 글자 200% 확대에서도 내용이나 기능 손실 없음
* 일반 텍스트 명암비 4.5:1 이상, 큰 글자와 주요 UI 3:1 이상
* 터치 영역 iOS 44×44pt, Android 48×48dp 이상
* 성공·실패·희귀도·기한을 색상만으로 구분하지 않음
* 시스템 모션 감소 설정 시 뽑기·진화·실패 애니메이션을 정적 화면으로 대체하고 건너뛰기 제공
* 초당 3회 이상 점멸 효과 금지
* 파트너 결과 선택·최종 승인 화면에서 남은 시간을 텍스트로 알리고 24시간 기한 안에는 언제든 재진입 가능

참고 기준은 [개인정보 보호법 제22조의2](https://www.law.go.kr/LSW/lsLinkCommonInfo.do?chrClsCd=010202&lsJoLnkSeq=1006183947)와 [WCAG 2.2](https://www.w3.org/TR/WCAG22/)다.

---

# 28. 게임 밸런스 위험과 해결책

> 결정 상태·출시 범위: 표의 `적용 상태` 열을 기준으로 판단

| 위험 | 해결 방법 | 적용 상태 |
| --- | --- | --- |
| 너무 쉬운 약속으로 코인 파밍 | 난이도별 보너스율·저난도 일일 보너스 상한·일 2개 서버 예약 한도 | `[확정][MVP]` |
| 판정 거절·최종 승인 누락으로 손실 회피 | `INVALID` 원금 반환을 별도 기록하고 7일 3회면 신규 코인 베팅 일시 중지 | `[확정][MVP]` |
| 파트너가 반복적으로 최종 승인하지 않음 | 거래성 알림·인앱 대기함, 반복 무효 시 베팅 중지, 연결 종료·차단 | `[확정][MVP]` |
| 강한 사람이 PvP를 계속 이김 | 선택 상성, 능력치 보정, 패배 보상 | `[제안][후속]` |
| 코인을 전부 잃음 | P0 구조 퀘스트 | `[확정][MVP]` |
| 뽑기 중복으로 흥미 저하 | MVP 자동 조각, P2 보관·교환 선택, 후속 강화 | 조각 `[확정][MVP]`, 보관·교환 `[확정][P2]`, 강화 `[제안][후속]` |
| 교환·선물 몰아주기와 다계정 악용 | 현재 커플만 허용하고 교환은 연결 7일 이후 개방, 중복 여분·등급·귀속·주간 한도·재이전 금지 적용 | `[확정][P2]` |
| 뽑기 운이 지나치게 나쁨 | 천장 시스템 | `[확정][MVP]` |
| 경쟁 때문에 실제로 싸움 | 무보상 비교, 간이 협동 보스, 비난 없는 문구 | `[확정][MVP]` |
| 콘텐츠가 빠르게 소모됨 | 시즌, 도감, 추가 진화 형태, 한정 보스 | `[제안][후속]` |
| 개발 범위가 너무 커짐 | MVP는 자동 판정 간이 보스, 선택형 PvP·정식 레이드는 후속 | `[확정][공통]` |
| 도박 앱처럼 보임 | 현금 기능 제거, 귀여운 퀘스트 중심 UI | `[확정][공통]` |
| 연결 종료 후 사생활 노출 | 즉시 접근 차단, 인증 자료 삭제 수명주기 | `[확정][공통]` |
| 인증 이미지 메타데이터 노출 | 비공개 저장, 재인코딩, EXIF·GPS 제거 | `[확정][공통]` |

---

# 29. MVP 범위

> 결정 상태: `[확정]` · 출시 범위: `[MVP]`

1차 버전에는 커플 연결과 안전한 종료, 퀘스트 버전별 시작 승인·최종 정산, 경험치·계정 레벨업, 영구 인정 성공 수 기반 4단계 배츄 외형 성장과 60회 숙련 보상, 세 종류의 뽑기, 장비·전투력, 파트너 비교 카드와 자동 판정 간이 협동 보스까지 포함한다. 80회 달성 선택권은 MVP에서 기록하되, 복수 배츄 보관함·선택·교체는 후속 P2에서 연다. 선택형 PvP·정식 레이드·월간 시즌과 고급 진화 애니메이션은 후속 버전으로 분리한다.

## MVP 필수 기능

| 우선순위 | 기능              |
| ---- | --------------- |
| P0   | 회원가입            |
| P0   | 커플 초대·미리보기·연결  |
| P0   | 연결 종료·차단·신고·탈퇴·내보내기 |
| P0   | 스타팅 몬스터 선택·이름 설정 |
| P0   | 코인 지갑           |
| P0   | 구조 퀘스트          |
| P0   | 퀘스트 생성          |
| P0   | 초안·회수·불변 버전·시작 승인 마감·수정 요청·거절 |
| P0   | 코인 잠금           |
| P0   | 파트너 성공·실패 예상과 시작 승인 |
| P0   | 파트너 실제 결과 선택·최종 승인·무효 처리 |
| P0   | 코인 정산           |
| P0   | 거래성 푸시·인앱 대기함  |
| P0   | 경험치·레벨업         |
| P0   | 배츄 인정 성공 수·4단계 외형 성장·60회 숙련 보상 |
| P0   | 연속 성공           |
| P0   | 일반·고급·전설 뽑기     |
| P0   | 아이템 인벤토리        |
| P0   | 장비 3종 장착        |
| P0   | 전투력 계산          |
| P0   | 파트너 성장 비교 카드   |
| P0   | 케미 게이지·간이 협동 보스 |
| P0   | 인증 자료 보호·삭제 수명주기 |

## MVP 직후 개선 기능

> 결정 상태: `[확정]` · 출시 범위: `[후속]` · 우선순위: `P1`

| 우선순위 | 기능 |
| --- | --- |
| P1   | 퀘스트 기록          |
| P1   | 일일 출석           |
| P1   | 이벤트·홍보 알림       |
| P1   | 아이템 도감          |

## MVP 출시 차단 비기능 요건

> 결정 상태: `[확정]` · 출시 범위: `[MVP]` · 우선순위: `P0`

| 분야 | 완료 기준 |
| --- | --- |
| 코인·뽑기 정합성 | 잠금·정산·뽑기 트랜잭션, 멱등 키와 DB 고유 제약 검증 |
| 계정 안전 | 연결 종료·차단·탈퇴 시 즉시 접근 차단과 최종 승인 여부 기준 취소·무효·원금 반환 검증 |
| 개인정보 | 비공개 인증 저장, 권한 검사, 보관·삭제·내보내기 검증 |
| 연령·동의 | 만 14세 이상 정책과 동의 버전 기록 |
| 접근성 | 27.4절 기준과 모션 감소·스크린리더 실기기 점검 |
| 알림 복구 | 푸시 거부 상태에서도 인앱 대기함으로 모든 필수 작업 가능 |

## MVP에서 제외

> 결정 상태: `[확정]` · 출시 범위: `[후속]`

* PvP 실제 전투
* 공격·방어·스킬 선택형 정식 레이드
* 아이템 강화
* 고급 진화 애니메이션 (진화 상태 전환과 단계별 정적 이미지 반영은 MVP 포함)
* 월간 시즌
* 커플 프로필 공유
* 배츄 여러 마리 보유·80회 알 선택·활성 배츄 교체 (`P2`)
* 중복 아이템 1:1 교환 (`P2-2`)
* 꾸미기 상점 선물·보유 아이템 선물 (`P2-1`, `P2-3`)
* 커플 방 꾸미기
* 유료 결제

> 결정 상태: `[확정]` · 출시 범위: `[MVP]`

MVP 배틀 탭에는 간이 협동 보스를 제공하고 PvP만 후속으로 예고한다.

```text
CO-OP

이번 주 케미 8 / 10
사용자 1 전투력 2,830
사용자 2 전투력 3,120

[간이 협동 보스]

PvP는 추후 업데이트 예정
```

---

# 30. MVP 콘텐츠 최소 수량

> 결정 상태: `[확정]` · 출시 범위: `[MVP]`

| 콘텐츠        |    최소 수량 |
| ---------- | -------: |
| 스타팅 배츄       |       4종 |
| 몬스터 성장 단계  | 알 포함 4단계 |
| 기본 퀘스트 템플릿 |      20개 |
| 무기         |       8개 |
| 방어구        |       8개 |
| 뽑기 장신구     |       8개 |
| 60회 숙련 장신구 |       4개 |
| 총 아이템      |      28개 |
| 아이템 등급     |      5단계 |
| 종별 숙련 오라    |       4개 |
| 종별 숙련 칭호    |       4개 |
| 간이 협동 보스   |       1종 |
| 실패 대사      | 카테고리별 3개 |
| 성공 대사      | 카테고리별 3개 |
| 기본 업적      | MVP 0개, 후속 10개 |

아트 범위를 줄이기 위해 알 실루엣은 공통으로 사용하고 종별 표식만 바꾼다. 아기부터는 별빛·파도·노을·숲의 팔레트를 각각 적용한다.

```text
공통 알 1개
아기 배츄 4개
중간 성장 배츄 4개
최종 진화 4개

총 13개 배츄 이미지
```

## 후속 P2 콘텐츠 수량 계획

> 결정 상태: 표의 `결정 상태` 열 기준 · 출시 범위: `[후속]` · 우선순위: `P2`

| 콘텐츠 | 수량 계획 | 결정 상태 |
| --- | ---: | --- |
| 정식 레이드 일반 보스 | 3종 | `[확정]` |
| 정식 레이드 시즌 보스 | 시즌당 1종 | `[확정]` |
| 80회 알 선택 후보 | 미보유 종 최대 3개 | `[확정]` |
| 상점 귀속 꾸미기 선물 | 가격·에셋 버전 검수 후 별도 확정 | `[미정]` |

---

# 31. 데이터베이스 구조

> 결정 상태: `[확정]` · 출시 범위: `[MVP]`

```text
users
- id
- nickname
- profile_image
- age_eligible_confirmed_at
- tutorial_completed_at
- signup_grant_issued_at
- status (PENDING_ELIGIBILITY, ACTIVE, DELETION_PENDING, DELETED)
- deleted_at
- created_at

auth_sessions
- id
- user_id
- refresh_token_hash (UNIQUE)
- status (ACTIVE, REVOKED, EXPIRED)
- expires_at
- revoked_at
- created_at

reauth_grants
- id
- user_id
- token_hash (UNIQUE)
- purpose (ACCOUNT_DELETION)
- expires_at
- consumed_at
- created_at

account_deletion_jobs
- id
- user_id
- reauth_grant_id
- relationship_end_job_id
- status (PENDING, PROCESSING, COMPLETED)
- idempotency_key
- requested_at
- evidence_delete_by
- personal_data_delete_by
- completed_at
- UNIQUE (user_id, idempotency_key)
- PARTIAL UNIQUE (user_id) WHERE status IN (PENDING, PROCESSING)

policy_versions
- id
- policy_type (TERMS, PRIVACY, EVIDENCE_OPTIONAL)
- policy_version
- locale
- required
- effective_at
- status (DRAFT, ACTIVE, RETIRED)
- UNIQUE (policy_type, policy_version, locale)
- policy_type·locale별 `ACTIVE` 1개 부분 고유 제약

policy_consents
- id
- user_id
- policy_version_id (FK policy_versions.id)
- agreed_at
- withdrawn_at
- UNIQUE (user_id, policy_version_id)

couples
- id
- status (CONNECTED, ENDED)
- connected_at
- ended_at
- ended_by
- betting_suspended_at
- betting_suspension_reason
- betting_risk_window_started_at
- betting_suspension_generation

relationship_end_jobs
- id
- couple_id (UNIQUE)
- initiated_by
- status (PENDING, PROCESSING, COMPLETED)
- target_resource_count
- processed_resource_count
- idempotency_key
- created_at
- completed_at
- UNIQUE (couple_id, idempotency_key)

relationship_end_job_items
- relationship_end_job_id
- resource_type (QUEST, ITEM_EXCHANGE, ITEM_GIFT, WEEKLY_RAID)
- resource_id
- status (PENDING, PROCESSING, COMPLETED)
- processed_at
- PRIMARY KEY (relationship_end_job_id, resource_type, resource_id)
- 모든 item이 COMPLETED이고 processed_resource_count = target_resource_count일 때만 상위 job을 COMPLETED로 전환

couple_invites
- id
- inviter_id
- code_hmac (UNIQUE)
- status (ACTIVE, PENDING_CONFIRMATION, USED, REVOKED, REJECTED, EXPIRED)
- expires_at
- claimed_by
- claimed_at
- inviter_confirmed_at
- invitee_confirmed_at
- used_by
- used_at
- revoked_at
- created_at
- PARTIAL UNIQUE (inviter_id) WHERE status IN (ACTIVE, PENDING_CONFIRMATION)
- PARTIAL UNIQUE (claimed_by) WHERE status = PENDING_CONFIRMATION

couple_pairing_slots
- user_id (PRIMARY KEY)
- invite_id
- role (INVITER, INVITEE)
- expires_at

couple_members
- couple_id
- user_id
- status (ACTIVE, ENDED)
- joined_at
- left_at
- PRIMARY KEY (couple_id, user_id)
- 활성 멤버십 기준 user_id UNIQUE

couple_rule_acknowledgements
- couple_id
- user_id
- suspension_generation
- rule_version
- acknowledged_at
- idempotency_key
- PRIMARY KEY (couple_id, user_id, suspension_generation)
- UNIQUE (couple_id, user_id, idempotency_key)

couple_betting_incidents
- couple_id
- quest_id (UNIQUE)
- incident_type (PARTNER_JUDGMENT_REJECTED, PARTNER_FINAL_APPROVAL_REJECTED, RESULT_CONFIRMATION_TIMEOUT)
- occurred_at
- PRIMARY KEY (couple_id, quest_id)

user_blocks
- blocker_id
- blocked_id
- created_at
- UNIQUE (blocker_id, blocked_id)
- CHECK blocker_id <> blocked_id

push_devices
- id
- user_id
- platform
- token_ciphertext
- token_fingerprint (UNIQUE)
- status
- last_seen_at
- revoked_at
- created_at

notification_settings
- user_id (PRIMARY KEY)
- lock_screen_preview_enabled (DEFAULT FALSE)
- transactional_push_enabled (DEFAULT TRUE)
- optional_push_enabled (DEFAULT FALSE)
- updated_at

notification_outbox
- id
- user_id
- event_type
- reference_type
- reference_id
- scheduled_at
- status
- attempts
- idempotency_key (UNIQUE)
- created_at

reports
- id
- reporter_id
- reported_user_id
- quest_id
- evidence_asset_id
- category
- description
- status
- idempotency_key
- created_at
- UNIQUE (reporter_id, idempotency_key)

user_progressions
- user_id (PRIMARY KEY)
- level
- current_level_exp
- base_hp
- base_attack
- streak
- updated_at
- CHECK level BETWEEN 1 AND 50
- CHECK current_level_exp >= 0

monsters
- id
- user_id
- name (NOT NULL)
- species (STARLIGHT, WAVE, SUNSET, FOREST)
- acquisition_source (STARTER, EGG_CHOICE)
- source_egg_choice_offer_id (NULL 허용, EGG_CHOICE이면 필수·UNIQUE)
- growth_stage (EGG, BABY, INTERMEDIATE, FINAL)
- recognized_success_count
- hatched_at
- intermediate_at
- final_at
- created_at
- CHECK recognized_success_count >= 0
- acquisition_source = STARTER인 user_id당 1개만 허용하는 부분 고유 제약

user_active_monsters
- user_id (PRIMARY KEY)
- monster_id (UNIQUE)
- selected_at
- monster_id의 소유자가 user_id와 같아야 함

monster_growth_milestone_claims
- id
- monster_id
- milestone (HATCH, SUCCESS_20, SUCCESS_40, SUCCESS_60, SUCCESS_80)
- source_quest_settlement_id
- granted_at
- UNIQUE (monster_id, milestone)
- source_quest_settlement_id는 해당 배츄에 인정된 정산만 참조

cosmetic_definitions
- id
- cosmetic_type (AURA, TITLE)
- species
- name
- asset_key
- transferable (DEFAULT FALSE)

user_cosmetics
- user_id
- cosmetic_definition_id
- source_milestone_claim_id
- acquired_at
- equipped_at
- PRIMARY KEY (user_id, cosmetic_definition_id)
- 종별 오라·칭호 같은 양도 불가 프로필 보상만 저장하며, 선물 가능한 꾸미기는 item_definitions와 inventory_item_units를 사용

[P2] egg_choice_offers
- id
- user_id
- source_monster_id
- milestone_claim_id (UNIQUE)
- status (PENDING_SELECTION, SELECTED, PENDING_CONTENT)
- options_version
- selected_species
- selection_idempotency_key (NULL 허용, 값이 있으면 UNIQUE)
- created_at
- selected_at

[P2] egg_choice_options
- egg_choice_offer_id
- options_version
- species
- display_order
- PRIMARY KEY (egg_choice_offer_id, options_version, species)
- 제시 시점 미보유 종만 최대 3개

wallets
- id
- user_id (UNIQUE)
- available_balance
- locked_balance
- updated_at
- CHECK available_balance >= 0
- CHECK locked_balance >= 0

wallet_transactions
- id
- wallet_id
- transaction_type
- reference_type
- reference_id
- available_delta
- locked_delta
- available_balance_after
- locked_balance_after
- idempotency_key
- created_at
- UNIQUE (wallet_id, idempotency_key)
- UNIQUE (wallet_id, reference_type, reference_id, transaction_type)

daily_reward_budgets
- user_id
- budget_date_kst
- coin_quest_count
- reserved_bonus
- issued_bonus
- reserved_low_difficulty_bonus
- issued_low_difficulty_bonus
- PRIMARY KEY (user_id, budget_date_kst)
- CHECK coin_quest_count BETWEEN 0 AND 2
- CHECK reserved_bonus >= 0
- CHECK issued_bonus >= 0
- CHECK reserved_bonus + issued_bonus <= 300
- CHECK reserved_low_difficulty_bonus >= 0
- CHECK issued_low_difficulty_bonus >= 0
- CHECK reserved_low_difficulty_bonus + issued_low_difficulty_bonus <= 100
- CHECK reserved_low_difficulty_bonus <= reserved_bonus
- CHECK issued_low_difficulty_bonus <= issued_bonus

user_quest_counters
- user_id (PRIMARY KEY)
- active_personal_quest_count
- updated_at
- CHECK active_personal_quest_count BETWEEN 0 AND 5

daily_activity_budgets
- user_id
- activity_date_kst
- xp_quest_count
- PRIMARY KEY (user_id, activity_date_kst)
- CHECK xp_quest_count BETWEEN 0 AND 5

quest_templates
- id
- category
- title
- success_criteria
- difficulty
- minimum_duration_minutes
- evidence_method (NONE, OPTIONAL_PHOTO)
- status (ACTIVE, RETIRED)

quests
- id
- couple_id
- creator_id
- quest_type (PERSONAL, COUPLE, LONG_TERM, BOSS, TUTORIAL, RESCUE)
- source_type (TEMPLATE, CUSTOM, SYSTEM)
- template_id (NULL 허용)
- status (DRAFT, PENDING_APPROVAL, CHANGE_REQUESTED, ACTIVE, AWAITING_RESULT, PENDING_FINAL_APPROVAL, SUCCESS, FAILURE, APPROVAL_EXPIRED, REJECTED, DISCARDED, CANCELED, INVALID, CANCELED_RELATIONSHIP_ENDED)
- row_version
- current_quest_version_id (NULL 허용)
- approved_quest_version_id (NULL 허용)
- approved_at
- reward_reserved_amount
- activity_slot_reserved_at
- xp_slot_date_kst
- coin_slot_reserved_at
- discarded_at
- canceled_reason
- created_at
- 초안 PATCH를 포함한 모든 성공 상태 변경과 마감 스케줄러 전환은 같은 트랜잭션에서 row_version = row_version + 1
- FK (id, current_quest_version_id) → quest_versions(quest_id, id)
- FK (id, approved_quest_version_id) → quest_versions(quest_id, id)
- 사용자 생성 PERSONAL 퀘스트의 PENDING_APPROVAL·CHANGE_REQUESTED·REJECTED·APPROVAL_EXPIRED는 current_quest_version_id NOT NULL, approved_quest_version_id NULL, approved_at NULL
- 사용자 생성 PERSONAL 퀘스트의 DRAFT·DISCARDED는 최초 작성이면 current_quest_version_id NULL, 회수·폐기 전 제출 이력이 있으면 NOT NULL이며 approved_quest_version_id와 approved_at은 항상 NULL
- 사용자 생성 PERSONAL 퀘스트의 ACTIVE·AWAITING_RESULT·PENDING_FINAL_APPROVAL·SUCCESS·FAILURE·CANCELED·INVALID는 approved_quest_version_id = current_quest_version_id이며 둘 다 NOT NULL, approved_at NOT NULL
- CANCELED_RELATIONSHIP_ENDED는 승인 전 종료면 approved_quest_version_id·approved_at NULL, 승인 후 종료면 approved_quest_version_id = current_quest_version_id이며 approved_at NOT NULL

quest_drafts
- quest_id (PRIMARY KEY)
- title
- category
- success_criteria
- difficulty
- stake
- due_at
- result_at
- minimum_duration_minutes
- evidence_method (NONE, OPTIONAL_PHOTO)
- revision_reason (INITIAL, CREATOR_RECALL, PARTNER_CHANGE_REQUEST)
- updated_at
- 작성자만 조회, `ACTIVE` 전환 후 수정 금지

quest_versions
- id
- quest_id
- version_no
- title
- category
- success_criteria
- difficulty
- stake
- due_at
- result_at
- minimum_duration_minutes
- approval_deadline_at
- result_confirmation_deadline_at
- evidence_method (NONE, OPTIONAL_PHOTO)
- submitted_by
- submitted_at
- UNIQUE (quest_id, version_no)
- UNIQUE (quest_id, id)
- 제출 후 UPDATE·DELETE 금지

quest_approvals
- id
- quest_id
- quest_version_id
- partner_id
- decision (APPROVE, REQUEST_CHANGE, REJECT)
- predicted_result (SUCCESS, FAILURE, APPROVE일 때만 필수)
- request_message
- decided_at
- UNIQUE (quest_id, partner_id, quest_version_id)
- FK (quest_id, quest_version_id) → quest_versions(quest_id, id)
- APPROVE는 predicted_result NOT NULL, REQUEST_CHANGE·REJECT는 predicted_result NULL이며 판단 행은 삽입 후 수정·삭제 금지

quest_recall_events
- id
- quest_id
- quest_version_id
- requested_by
- idempotency_key
- recalled_at
- UNIQUE (quest_id, idempotency_key)
- FK (quest_id, quest_version_id) → quest_versions(quest_id, id)

quest_cancel_requests
- id
- quest_id
- requested_by
- requested_at
- responded_by
- responded_at
- response_decision (CONFIRM, REJECT)
- status (PENDING, CONFIRMED, REJECTED, EXPIRED)
- active PENDING 요청 기준 quest_id UNIQUE

quest_partner_results
- id
- quest_id (UNIQUE)
- partner_id
- selected_result (SUCCESS, FAILURE, 선택 전 NULL 허용)
- selection_revision
- selected_at
- final_decision (APPROVE, REJECT, 결정 전 NULL 허용)
- final_decided_at
- invalid_reason (PARTNER_JUDGMENT_REJECTED, PARTNER_FINAL_APPROVAL_REJECTED, RESULT_CONFIRMATION_TIMEOUT, SYSTEM_ERROR, RELATIONSHIP_ENDED_BEFORE_FINAL_APPROVAL, 그 밖에는 NULL)
- resolved_at
- `ACTIVE → AWAITING_RESULT` 전환 때 선택값이 비어 있는 요약 행을 생성해 최초 선택 전 마감도 같은 행에서 처리
- selected_result가 NULL이면 selection_revision = 0·selected_at = NULL, 값이 있으면 selection_revision >= 1·selected_at NOT NULL
- final_decision = APPROVE이면 selected_result·final_decided_at·resolved_at NOT NULL이고 invalid_reason NULL
- final_decision = REJECT이면 final_decided_at·invalid_reason·resolved_at NOT NULL

quest_partner_result_events
- id
- quest_partner_result_id
- actor_user_id (스케줄러·시스템 이벤트는 NULL 허용)
- event_type (SELECT, CHANGE, JUDGMENT_REJECT, FINAL_APPROVE, FINAL_REJECT, TIMEOUT_INVALIDATE, SYSTEM_INVALIDATE, RELATIONSHIP_INVALIDATE)
- selected_result (SELECT·CHANGE·FINAL_APPROVE일 때 필수, 그 밖에는 NULL 허용)
- idempotency_key (UNIQUE)
- created_at
- 선택 변경 이력을 포함해 추가만 허용하며 UPDATE·DELETE 금지

quest_settlements
- id
- quest_id (UNIQUE)
- resolved_result
- resolution_source
- invalid_reason (INVALID일 때 필수, 그 밖에는 NULL)
- stake
- reward
- wallet_transaction_id
- credited_monster_id (NULL 허용)
- recognized_success_before
- recognized_success_after
- growth_stage_before
- growth_stage_after
- settled_at
- 일반 PERSONAL SUCCESS는 credited_monster_id와 전후 성공 수·성장 단계가 NOT NULL이고 recognized_success_after = recognized_success_before + 1
- 튜토리얼 SUCCESS는 credited_monster_id NOT NULL, 인정 성공 전후 수 NULL, growth_stage_before = EGG, growth_stage_after = BABY
- 그 밖의 결과는 인정 성공 전후 수가 NULL

rescue_quest_claims
- id
- user_id
- claimed_at
- reward
- idempotency_key
- UNIQUE (user_id, idempotency_key)

evidence_assets
- id
- quest_id (NOT NULL)
- uploader_id
- object_key
- mime_type
- byte_size
- checksum
- processing_status (UPLOADING, PROCESSING, READY, FAILED, BLOCKED, DELETED)
- retention_reason (NORMAL, SAFETY_REPORT)
- hold_until
- storage_scope (NORMAL, RESTRICTED_PENDING, RESTRICTED)
- submitted_for_review_at (NULL 허용, 파트너 판정에 제출할 때 기록)
- created_at
- expires_at
- deletion_requested_at
- deletion_due_at
- deleted_at

evidence_access_logs
- id
- evidence_asset_id
- actor_id
- action
- created_at

item_definitions
- id
- name
- rarity
- item_type (EQUIPMENT, COSMETIC)
- slot (WEAPON, ARMOR, ACCESSORY, NONE)
- cosmetic_slot (HEAD, BODY, BACKGROUND, NONE)
- gift_duplicate_policy (REJECT_IF_OWNED, ALLOW)
- species (NULL 허용, 종별 숙련 보상일 때 필수)
- attack_bonus
- hp_bonus
- exchange_value_group (NULL 허용)
- is_limited
- is_exchangeable
- is_giftable
- asset_key
- 선물 가능한 COSMETIC은 hp_bonus = 0, attack_bonus = 0, cosmetic_slot <> NONE, gift_duplicate_policy = REJECT_IF_OWNED

inventory_item_units
- id
- owner_id
- item_definition_id
- origin_source (GACHA, MILESTONE, SHOP_GIFT)
- binding_type (UNBOUND, MILESTONE_BOUND, TRANSFER_LOCKED)
- state (AVAILABLE, EQUIPPED, PENDING_DISPOSITION, RESERVED_EXCHANGE, RESERVED_GIFT, CONVERTED)
- enhancement_level
- source_milestone_claim_id (NULL 허용)
- acquired_at
- transferred_at
- `CONVERTED`는 장착·예약·이전 불가
- source_milestone_claim_id가 NULL이 아닌 단위는 (source_milestone_claim_id, item_definition_id) UNIQUE

item_unit_reservations
- id
- inventory_item_unit_id
- reservation_type (EXCHANGE, GIFT)
- reference_id
- status (ACTIVE, RELEASED, CONSUMED)
- created_at
- released_at
- consumed_at
- ACTIVE인 inventory_item_unit_id당 1개만 허용하는 부분 고유 제약

item_fragments
- user_id
- item_definition_id
- quantity
- PRIMARY KEY (user_id, item_definition_id)
- CHECK quantity >= 0

equipment
- user_id (PRIMARY KEY)
- weapon_item_unit_id
- armor_item_unit_id
- accessory_item_unit_id
- 각 단위의 owner_id가 user_id와 같고 state = EQUIPPED여야 함

gacha_probability_versions
- id
- gacha_type
- version
- status
- effective_from
- UNIQUE (gacha_type, version)
- gacha_type별 status = ACTIVE 행 1개만 허용하는 부분 고유 제약

gacha_probability_entries
- probability_version_id
- item_definition_id
- weight
- UNIQUE (probability_version_id, item_definition_id)
- CHECK weight > 0

gacha_pity_counters
- user_id
- gacha_type
- misses_since_target
- updated_at
- PRIMARY KEY (user_id, gacha_type)
- CHECK misses_since_target >= 0

gacha_draws
- id
- user_id
- gacha_type
- payment_type (COIN, TICKET)
- consumable_type
- cost
- item_definition_id
- probability_version_id
- pity_count_before
- pity_count_after
- pity_applied
- was_duplicate
- result_item_unit_id (MVP 자동 조각 중복만 NULL, 그 밖에는 생성된 개별 단위)
- duplicate_disposition (NOT_APPLICABLE, AUTO_FRAGMENTED, PENDING, KEPT, CONVERTED, EXPIRED_CONVERTED)
- fragment_amount
- wallet_transaction_id
- idempotency_key
- created_at
- UNIQUE (user_id, idempotency_key)
- result_item_unit_id는 NULL이 아닐 때 UNIQUE
- wallet_transaction_id는 NULL이 아닐 때 UNIQUE

[P2] duplicate_item_dispositions
- gacha_draw_id (PRIMARY KEY)
- inventory_item_unit_id (UNIQUE)
- decision (NULL 허용, KEEP_FOR_EXCHANGE, CONVERT_TO_FRAGMENTS)
- status (PENDING, COMPLETED, EXPIRED_CONVERTED)
- expires_at
- decided_at (NULL 허용)
- idempotency_key (PENDING에서는 NULL 허용, 값이 있으면 UNIQUE)
- PENDING은 decision·decided_at·idempotency_key NULL, 사용자 선택 COMPLETED는 셋 다 NOT NULL, EXPIRED_CONVERTED는 decision·decided_at만 NOT NULL

[P2] item_exchange_offers
- id
- couple_id
- proposer_id
- status (WAITING_PARTNER_ITEM, AWAITING_CONFIRMATIONS, COMPLETED, CANCELED, REJECTED, EXPIRED, CANCELED_RELATIONSHIP_ENDED)
- current_offer_version
- reserved_quota_week_start_at_kst
- completed_quota_week_start_at_kst (NULL 허용)
- row_version
- expires_at
- completed_at
- created_at
- idempotency_key
- UNIQUE (proposer_id, idempotency_key)
- expires_at은 최초 created_at + 24시간으로 고정하고 버전 변경으로 연장하지 않음

[P2] item_exchange_versions
- id
- item_exchange_offer_id
- version_no
- changed_by
- requested_item_definition_id
- created_at
- UNIQUE (item_exchange_offer_id, version_no)

[P2] item_exchange_version_items
- item_exchange_version_id
- owner_id
- inventory_item_unit_id
- PRIMARY KEY (item_exchange_version_id, owner_id)
- UNIQUE (item_exchange_version_id, inventory_item_unit_id)

[P2] item_exchange_confirmations
- item_exchange_version_id
- user_id
- confirmed_at
- PRIMARY KEY (item_exchange_version_id, user_id)

[P2] weekly_item_exchange_budgets
- couple_id
- week_start_at_kst
- reserved_count
- completed_count
- PRIMARY KEY (couple_id, week_start_at_kst)
- CHECK reserved_count >= 0 AND completed_count >= 0
- CHECK reserved_count + completed_count <= 3

[P2] shop_offering_versions
- id
- item_definition_id
- price
- status (ACTIVE, RETIRED)
- effective_from
- UNIQUE (item_definition_id, effective_from)
- item_definition_id별 status = ACTIVE 행 1개만 허용하는 부분 고유 제약
- 가격·아이템·effective_from은 사용 후 수정 금지, status는 ACTIVE → RETIRED 단방향 변경만 허용
- 전투력 0·선물 중복 거절·유효 cosmetic_slot 꾸미기만 등록 가능

[P2] item_gifts
- id
- couple_id
- sender_id
- recipient_id
- gift_type (SHOP_COSMETIC, INVENTORY_ITEM)
- shop_offering_version_id (NULL 허용)
- inventory_item_unit_id (NULL 허용)
- price_snapshot (보유 아이템 선물은 NULL)
- status (PENDING, ACCEPTED, REJECTED, REJECTED_SETTINGS_DISABLED, CANCELED, EXPIRED, CANCELED_RELATIONSHIP_ENDED)
- row_version
- reserved_quota_week_start_at_kst (보유 아이템 선물만, 그 밖에는 NULL)
- completed_quota_week_start_at_kst (NULL 허용)
- wallet_lock_transaction_id (NULL 허용)
- wallet_charge_transaction_id (NULL 허용)
- wallet_refund_transaction_id (NULL 허용)
- expires_at
- accepted_at
- created_at
- idempotency_key
- UNIQUE (sender_id, idempotency_key)
- gift_type에 따라 상점 버전 또는 개별 아이템 중 정확히 하나만 필수

[P2] weekly_item_gift_budgets
- sender_id
- week_start_at_kst
- reserved_count
- completed_count
- PRIMARY KEY (sender_id, week_start_at_kst)
- CHECK reserved_count >= 0 AND completed_count >= 0
- CHECK reserved_count + completed_count <= 1
- P2-3 보유 아이템 선물에만 사용

[P2] user_interaction_settings
- user_id (PRIMARY KEY)
- item_gifts_enabled (DEFAULT TRUE)
- gifts_disabled_at (NULL 허용)
- row_version
- updated_at

[P2] gift_settings_cleanup_jobs
- id
- user_id
- settings_row_version
- status (PENDING, PROCESSING, COMPLETED)
- target_gift_count
- processed_gift_count
- idempotency_key
- created_at
- completed_at
- UNIQUE (user_id, settings_row_version)
- UNIQUE (user_id, idempotency_key)

[P2] gift_settings_cleanup_items
- gift_settings_cleanup_job_id
- item_gift_id
- status (PENDING, PROCESSING, COMPLETED)
- processed_at
- PRIMARY KEY (gift_settings_cleanup_job_id, item_gift_id)

[P2] user_cosmetic_loadout_slots
- user_id
- cosmetic_slot (HEAD, BODY, BACKGROUND)
- inventory_item_unit_id (NULL 허용, 값이 있으면 UNIQUE)
- row_version
- equipped_at
- PRIMARY KEY (user_id, cosmetic_slot)
- 단위 owner_id = user_id, item_type = COSMETIC, 정의의 cosmetic_slot과 슬롯이 같아야 함
- 해제 시 행을 삭제하지 않고 inventory_item_unit_id·equipped_at만 NULL로 바꾸며 row_version 증가

[P2] item_transfer_events
- id
- inventory_item_unit_id
- transfer_type (EXCHANGE, SHOP_GIFT, INVENTORY_GIFT)
- from_user_id (NULL 허용)
- to_user_id
- reference_id
- created_at
- UNIQUE (transfer_type, reference_id, inventory_item_unit_id)

user_consumables
- user_id
- consumable_type
- quantity
- PRIMARY KEY (user_id, consumable_type)
- CHECK quantity >= 0

weekly_coop_bosses
- id
- couple_id
- week_start_at
- week_end_at
- required_power (두 스타팅 배츄 생성 전 NULL 허용)
- combined_power_snapshot (판정 전 NULL 허용)
- status (LOCKED, AVAILABLE, WAITING_PARTNER, CLEARED, FAILED, EXPIRED, CANCELED_RELATIONSHIP_ENDED)
- unlocked_at
- reward_granted_at
- resolved_at
- UNIQUE (couple_id, week_start_at)

weekly_coop_participants
- weekly_coop_boss_id
- user_id
- combat_power_snapshot (NULL 허용, 두 번째 참여 시 두 사람 모두 기록)
- participated_at
- PRIMARY KEY (weekly_coop_boss_id, user_id)

weekly_power_baselines
- couple_id
- user_id
- week_start_at
- combat_power
- captured_at
- PRIMARY KEY (couple_id, user_id, week_start_at)

[P2] boss_definitions
- id
- boss_key (UNIQUE)
- display_name
- boss_type (NORMAL, SEASONAL)
- status (ACTIVE, RETIRED)

[P2] boss_definition_versions
- id
- boss_definition_id
- version_no
- difficulty
- max_hp
- combat_config_json
- appearance_asset_key
- skill_set_json
- reward_table_json
- direction_data_json
- effective_from
- UNIQUE (boss_definition_id, version_no)
- CHECK max_hp > 0
- 사용된 버전은 수정·삭제 금지

[P2] raid_rotation_weeks
- week_start_at_kst (PRIMARY KEY)
- boss_definition_version_id
- rotation_slot (1, 2, 3, 4)
- season_key (NULL 허용)
- created_at
- CHECK rotation_slot BETWEEN 1 AND 4
- UNIQUE (week_start_at_kst, boss_definition_version_id)

[P2] weekly_raid_instances
- id
- couple_id
- raid_rotation_week_start_at_kst
- boss_definition_version_id
- status (AVAILABLE, IN_PROGRESS, CLEARED, FAILED, EXPIRED, CANCELED_RELATIONSHIP_ENDED)
- started_at
- resolved_at
- UNIQUE (couple_id, raid_rotation_week_start_at_kst)
- FK (raid_rotation_week_start_at_kst, boss_definition_version_id) → raid_rotation_weeks(week_start_at_kst, boss_definition_version_id)
- 생성 이후 boss_definition_version_id 변경 금지
```

## 중요한 백엔드 원칙

1. 잔액을 직접 수정하지 않고 모든 변화를 `wallet_transactions`에 기록한다. 원장 합계와 지갑 스냅샷은 항상 재검산 가능해야 한다.
2. 일반 개인 퀘스트는 `DRAFT` 생성 시 작성자 전용 `quest_drafts`만 저장하고 코인·활동·XP·보너스 슬롯을 예약하지 않는다. 수정은 `DRAFT` 또는 `CHANGE_REQUESTED`에서 예상 `row_version`이 맞을 때만 허용한다. 폐기는 작업 초안을 삭제하고 `DISCARDED`로 끝낸다. 제출 시 서버가 도전자의 `tutorial_completed_at`과 최소 수행 시간, `approval_deadline_at = due_at - minimum_duration_minutes`를 검증한다. 튜토리얼이 미완료면 `TUTORIAL_REQUIRED`, 마감이 미래가 아니면 기존 편집 상태를 유지하며, 직전 제출 버전과 서버 diff가 비어 있어도 `UNCHANGED_QUEST_VERSION`으로 거절한다. 유효한 제출은 불변 `quest_versions` 삽입, `current_quest_version_id`·상태·`row_version` 갱신과 outbox 기록을 한 트랜잭션으로 처리한다. 시작 승인·수정 요청·거절·회수는 `quests` 행을 잠그고 현재 버전과 `row_version`을 확인한 뒤 서버 시각을 먼저 검사한다. 마감과 같거나 늦으면 항상 `APPROVAL_EXPIRED`가 우선한다. 그 전에는 회수 이벤트 삽입과 `PENDING_APPROVAL → DRAFT`, 승인 판단 행 삽입과 `PENDING_APPROVAL → CHANGE_REQUESTED` 또는 `REJECTED`를 각각 상태·`row_version`·outbox와 한 트랜잭션으로 처리한다. 재제출한 새 버전은 이전 버전의 승인을 재사용하지 않는다. 파트너 시작 승인은 요청한 버전이 `current_quest_version_id`와 정확히 같고 `predicted_result`가 `SUCCESS` 또는 `FAILURE`일 때만 처리한다. 시작 승인 트랜잭션은 해당 `couples`·`quests` 행을 먼저 잠그고 `CONNECTED`인지, 도전자의 튜토리얼이 최종 종료됐는지, 활성 배츄가 존재하는지 재검사한 뒤 `user_quest_counters`와 결과 예정일의 `daily_activity_budgets`를 잠가 동시 진행 수와 XP 슬롯을 각각 1개 예약하고 `activity_slot_reserved_at`, `xp_slot_date_kst`를 기록한다. `stake > 0`이면 지갑·`daily_reward_budgets`도 잠가 난이도·출처와 남은 전체·저난도 예산으로 `reward_reserved_amount`를 확정하고, 같은 트랜잭션에서 예상값을 포함한 APPROVE 판단 행·`approved_at`·코인 슬롯·보너스 예약·코인 잠금·`coin_slot_reserved_at`·`approved_quest_version_id`·상태·`row_version`·원장·outbox를 처리한다. 0C는 코인 예산만 사용하지 않으며 튜토리얼·구조 퀘스트는 반복 활동 슬롯에서 제외한다.
3. `activity_slot_reserved_at`이 있는 일반 개인 퀘스트가 종료될 때만 활성 진행 수를 1 줄인다. 파트너가 최종 승인한 성공 정산은 `reward_reserved_amount`만 지급하고 전체 예약 보너스 및 해당 시 저난도 예약 보너스를 발행 칸으로 옮긴다. 파트너가 최종 승인한 실패·마감 전 공동 취소·마감 전 관계 종료는 예약 보너스만 해제하고 이미 사용한 코인·XP 일일 슬롯은 줄이지 않는다. `INVALID`는 실제 `coin_slot_reserved_at`과 `xp_slot_date_kst`가 있는 경우에만 해당 슬롯과 예약 보너스를 복구한다. 퀘스트·파트너 판정·지갑·활동 예산·코인 예산·원장·`quest_settlements` 중 하나라도 실패하면 전부 롤백한다.
4. `quest_settlements.quest_id UNIQUE`로 한 퀘스트를 한 번만 정산한다. 같은 요청이 반복되면 새 지급 없이 기존 정산 결과를 반환한다.
5. 퀘스트 제출·회수·폐기·수정 요청·거절·시작 승인·취소·실제 결과 선택·최종 승인·무효 처리·정산과 구조 퀘스트·뽑기·연결 종료·차단·탈퇴처럼 공유 상태, 코인 또는 접근 권한을 바꾸는 요청에는 `Idempotency-Key`를 필수로 받는다. 제출은 `expectedRowVersion`, 회수는 `questVersionId + expectedRowVersion`, 수정 요청·거절·시작 승인은 `questVersionId + expectedRowVersion`이 현재 값과 맞을 때만 처리한다. 결과 선택·수정·최종 승인·판정 거절은 `expectedRowVersion`과 현재 선택값을 검사한다. 초안 `PATCH`는 `Idempotency-Key` 대신 `expectedRowVersion`으로 덮어쓰기를 막는다. 초안 내용 변경을 포함해 `quests`에 영향을 주는 PATCH·제출·회수·수정 요청·거절·시작 승인·폐기·취소·결과 상태 변경과 마감 작업이 성공할 때마다 같은 트랜잭션에서 `row_version`을 정확히 1 올린다. 모든 퀘스트 읽기와 성공한 변경 응답은 새 `rowVersion`을 반환하고, 승인 대기 응답은 현재 `questVersionId`도 함께 반환한다.
6. 파트너의 실제 결과 선택·수정·판정 거절·최종 승인·최종 거절은 `quest_partner_result_events`에 추가만 하고 수정·삭제하지 않는다. 현재 작업 값은 `quest_partner_results`에 투영하되 예상 `row_version`과 파트너 역할을 검증한다. 최종 승인 트랜잭션은 선택된 결과를 다시 확인하고 `quest_settlements.quest_id UNIQUE`를 이용해 정확히 한 번만 `SUCCESS` 또는 `FAILURE`로 정산한다.
7. 시작 승인 때의 `predicted_result`는 `ACTIVE`부터 두 사람 모두에게 표시하되 실제 결과·보상 계산에는 사용하지 않는다. `AWAITING_RESULT`와 `PENDING_FINAL_APPROVAL`에서 도전자에게는 파트너의 선택·변경 버튼을 제공하지 않고 확인 대기 상태와 마감만 반환한다. 파트너가 선택한 실제 결과는 최종 확인 화면과 감사 로그에 사용하며 최종 승인 전에는 정산 결과로 표시하지 않는다.
8. 시작 승인·결과 확인·최종 승인·공동 취소 요청·초대·간이 보스 마감은 클라이언트 시간이 아니라 서버 시각과 재시도 가능한 스케줄러로 처리한다. `PENDING_APPROVAL`이 승인 마감에 도달하면 `APPROVAL_EXPIRED`로 한 번만 전환하고 코인·활동·XP·보너스 슬롯을 건드리지 않으며, 화면에는 `EXPIRED`로 표시한다. `AWAITING_RESULT` 또는 `PENDING_FINAL_APPROVAL`이 `result_confirmation_deadline_at`에 도달하면 한 번만 `INVALID`로 전환하고 잠긴 원금 반환·예약 및 슬롯 복구·무보상 처리를 같은 트랜잭션에서 커밋한다. P2에서는 중복 선택 24시간, 교환 24시간, 선물 72시간 만료와 월요일 정식 레이드 인스턴스 생성도 같은 스케줄러 계약을 사용한다. 마감 작업은 모두 결정적인 멱등 키와 고유 제약을 사용한다. 초대 생성·참여 시 `couple_pairing_slots`로 양쪽 사용자를 예약하고 종료 상태에서 해제해 여러 연결 요청에 동시에 들어가지 못하게 한다. 이전 커플의 `relationship_end_jobs`가 `COMPLETED`가 아니면 새 초대 참여와 연결 확정을 거절한다.
9. `POST /gacha/draw`는 사용자가 확인한 `expectedProbabilityVersionId`를 요구한다. 트랜잭션 시작 시 해당 뽑기 종류의 `ACTIVE` 버전을 잠그고 ID가 다르면 차감 없이 `409 Conflict`로 새 확률표 확인을 요구한다. 일치하면 코인 또는 뽑기권 차감, 그 버전으로 서버 추첨, 확률 버전 기록, 천장 갱신, 뽑기 기록, 최초 획득 개별 단위 생성 또는 중복 조각 증가를 한 트랜잭션으로 처리한다. P2 중복 보관 선택이 열린 뒤에는 중복 조각 증가 대신 `PENDING_DISPOSITION` 단위를 생성한다.
10. 사용된 확률 버전은 수정하지 않는다. 새 버전은 모든 가중치가 양수이고 합계·아이템 참조가 유효한지 활성화 트랜잭션에서 검사한 뒤에만 `ACTIVE`로 바꾼다. 천장은 뽑기 종류별로 잠그고 목표 등급 이상 획득 시에만 초기화하며, 확률 버전 변경으로 초기화하지 않는다.
11. MVP 중복 아이템의 조각 수량은 `item_fragments`에 원자적으로 누적하고 음수가 될 수 없다. 보유 판정은 `CONVERTED`를 제외한 현재 소유자의 같은 정의 개별 단위를 기준으로 하며, 뽑기 기록과 조각 증가 사이에 중간 커밋을 두지 않는다.
12. 전투력은 서버가 기본 능력치와 장착 아이템의 유효 보너스로 계산하며, 별도 `power` 값을 저장하거나 클라이언트에서 받지 않는다.
13. 케미 게이지는 별도 누적값으로 저장하지 않는다. 해당 주의 일반 개인 퀘스트 중 파트너의 최종 승인으로 정산된 `SUCCESS` 또는 `FAILURE`만 `COUNT(DISTINCT quest_id)`하고 `INVALID`, 튜토리얼·구조 퀘스트를 제외한다.
14. MVP 애플리케이션은 사용자당 스타팅 배츄 한 마리와 활성 배츄 한 마리만 허용한다. `POST /monsters/starter`는 `STARLIGHT`, `WAVE`, `SUNSET`, `FOREST` 중 선택한 종류와 이름, 계정 성장 행, 활성 배츄 참조를 한 트랜잭션에서 만들며 스타터 부분 고유 제약으로 동시 요청과 재시도의 중복 생성을 막는다. DB는 80회 보상의 소급 지급을 위해 복수 배츄를 수용하되, P2 전에는 추가 생성·선택·교체 API를 열지 않는다.
15. 계정 `user_progressions.base_hp`, `base_attack`은 장비를 제외한 현재 계정 레벨의 최대 기본 능력치이며 모든 배츄가 공유한다. 일반 개인 퀘스트가 파트너의 최종 승인으로 `SUCCESS` 정산되면 코인 처리와 함께 `user_progressions`·`user_active_monsters`·해당 `monsters`·마일스톤 행을 잠그고 XP·연승·레벨·기본 능력치를 갱신한 뒤, 잠근 활성 배츄에 인정 성공 수를 정확히 1회 기록한다. 이 잠금 뒤 `credited_monster_id`를 고정하므로 활성 교체와 정산이 경합해도 한 성공이 두 배츄에 들어가지 않는다. 튜토리얼·구조·보스 퀘스트와 `FAILURE`·`CANCELED`·`INVALID`는 인정 성공 수에서 제외한다. 튜토리얼 성공은 인정 성공 수를 올리지 않고 `(monster_id, HATCH)` 마일스톤과 `EGG → BABY`를 같은 정산에서 처리한다. 스타터인지 추가 알인지와 관계없이 활성 배츄가 아직 `EGG`라면 첫 인정 성공 정산이 수를 `0 → 1`로 올리는 동시에 같은 `HATCH` 마일스톤을 만들고 부화시킨다. 인정 성공 수가 처음 20·40·60·80에 도달한 정산에서 각각 `INTERMEDIATE`, `FINAL`, 종별 오라·칭호·전투력 0 귀속 장신구, `SUCCESS_80` 달성 기록을 고유 마일스톤으로 지급한다. 정산에는 `credited_monster_id`, 인정 성공 수와 성장 단계의 전후 값을 저장하고 일반 개인 성공은 전후 수가 정확히 1 차이 나야 한다. MVP는 80회에 마일스톤과 outbox 이벤트만 만든다. P2 배포 시의 소급 스캔과 이후 상시 작업자가 선택권이 없는 모든 `SUCCESS_80` 이벤트를 소비해 마일스톤당 한 번만 `egg_choice_offer`로 변환한다. 새 종이 활성화되면 `PENDING_CONTENT`도 다시 평가해 새 불변 후보 버전을 만들고 `PENDING_SELECTION`으로 전환한다. XP·성장·마일스톤 중 하나라도 실패하면 코인과 정산도 전부 롤백한다. 파트너가 최종 승인한 실패 정산은 계정 연승 초기화만 함께 처리한다.
16. 연결 종료·차단·탈퇴는 `couples` 행을 먼저 잠그고, 커플 `ENDED` 전환, 양쪽 `couple_members` 종료·`left_at` 기록, 관계 접근 차단, 미판정 주간 보스 취소와 `relationship_end_jobs` 생성을 먼저 독립 커밋한다. 후속 작업은 대상 퀘스트와 P2의 미완료 교환·선물·정식 레이드를 각각 `relationship_end_job_items`로 고정한 뒤, 6.5절의 반환·소각·예약 해제·잠금 코인 반환을 자원별로 멱등 처리한다. 각 자원 완료와 `processed_resource_count`를 함께 갱신하고 모든 자원이 끝나기 전에는 상위 작업을 `COMPLETED`로 바꾸지 않는다. 활성 멤버십 제약은 즉시 해제하되 종료 작업 완료 전에는 새 초대 참여와 연결 확정을 거절한다.
17. 인증 자료는 `quest_id`로 업로드를 시작하고 완료 호출 후 검사·재인코딩한다. `READY` 처리본만 미디어 게이트웨이가 매 요청 권한을 확인해 전달하며, 안전 신고 보존본은 제한 저장소와 `hold_until`로 분리한다.
18. 간이 협동 보스 참여는 `couples` 행을 먼저 잠그고 `CONNECTED`를 재검사한다. 첫 참여는 시각만 기록한다. 두 번째 참여 트랜잭션에서 커플·두 참가자·보스 행을 잠그고 양쪽 전투력을 동시에 저장해 한 번만 판정하며, 클리어한 때만 양쪽 뽑기권 지급과 결과 저장을 함께 처리한다.
19. 기존 커플은 월요일, 배츄를 이미 가진 새 커플은 연결 완료 시, 기준값이 없는 신규 사용자는 스타팅 배츄 생성 시 `weekly_power_baselines`를 관계별로 한 번 기록하고 `현재 전투력 - 기준 전투력`을 비교 카드의 `weeklyCombatPowerDelta`로 계산한다.
20. 상태 변경 트랜잭션은 같은 DB 트랜잭션에서 `notification_outbox`를 기록한다. 발송 작업자는 고유 `idempotency_key`로 재시도하며 푸시 실패가 비즈니스 정산을 반복시키지 않게 한다. 발송 직전에 연결·차단·설정 상태를 다시 확인하고 무효가 된 예약 알림은 취소한다. 로그아웃·토큰 갱신·탈퇴 시 기존 기기 토큰을 폐기한다.
21. 구조 퀘스트 완료 시 사용자·`wallets`·최근 `rescue_quest_claims` 행을 잠그고, 가용 잔액 100C 미만·잠긴 잔액 0C·최근 7일 1회·30일 2회 조건을 완료 트랜잭션 안에서 다시 검사한다. 조건이 유효할 때만 지급 원장과 청구 기록을 함께 커밋한다.
22. 튜토리얼은 `users.tutorial_completed_at`이 없는 사용자에게 한 번만 생성하고 `SUCCESS`, `FAILURE`, `INVALID` 또는 관계 종료 중 하나로 끝나면 완료 시각을 기록해 반복 생성하지 않는다. `FAILURE`·`INVALID`·관계 종료 시에는 잠긴 100C만 반환하며 배츄는 `EGG`로 남는다. `SUCCESS` 때만 최초 보너스 100C와 10XP를 지급하고, 활성 배츄 ID를 정산에 기록하며 인정 성공 수 변화 없이 `(monster_id, HATCH)` 고유 마일스톤과 `EGG → BABY`를 생성한다. 완료 시각·지갑·성장 단계·마일스톤·정산을 한 트랜잭션으로 처리한다. 성공하지 못한 사용자의 부화는 15번의 첫 인정 성공 규칙으로 보장한다.
23. 신규 로그인은 `PENDING_ELIGIBILITY` 사용자에 연결된 제한 온보딩 세션만 발급한다. 서버는 `policy_versions`의 현재 `ACTIVE`이며 `required = true`인 각 정책 버전을 기준으로 동의를 판단한다. 연령 기준 통과와 현재 필수 버전 동의가 모두 저장된 뒤에만 사용자를 `ACTIVE`로 바꾸고 일반 API를 허용한다. 만 14세 미만 응답·연령 확인 거절 시 모든 제한 세션을 즉시 `REVOKED`로 바꾸고 임시 사용자·OAuth 프로필을 24시간 이내 삭제한다. 로그아웃·소셜 계정 철회·탈퇴 시에도 관련 세션을 즉시 폐기하고 이후 액세스·리프레시 토큰을 거부한다.
24. 인증 업로드 URL 발급 시 활성 선택 동의, 현재 커플 권한, 퀘스트 상태가 `ACTIVE`, `AWAITING_RESULT`, `PENDING_FINAL_APPROVAL` 중 하나인지, 퀘스트당 3장·총 20MB 예약 한도를 한 트랜잭션에서 검사한다. 파트너 판정에 사용할 자료는 별도 제출 호출로 `submitted_for_review_at`을 기록하며 `READY` 상태만 허용한다. `UPLOADING`은 1시간, 완료되지 않았거나 제출되지 않은 임시 객체와 `FAILED` 원본은 최대 24시간 뒤 멱등 삭제한다.
25. 연령 기준과 필수 동의를 모두 통과해 사용자를 처음 `ACTIVE`로 바꾸는 트랜잭션에서 지갑 생성, `SIGNUP_GRANT` 1,000C 원장과 `signup_grant_issued_at`을 함께 기록한다. 사용자당 `SIGNUP_GRANT` 원장 고유 제약으로 재로그인·재연결·재시도가 중복 지급을 만들지 못하게 한다.
26. 장비 장착·해제 시 사용자 장비 행과 대상 `inventory_item_units`를 잠그고 소유자, `AVAILABLE`·`EQUIPPED` 상태, 귀속, 아이템 슬롯을 서버가 검사한다. 다른 사용자의 단위, 예약·처리 대기 단위 또는 슬롯이 다른 아이템은 장착할 수 없으며, 단위 상태·장비 참조 갱신과 새 전투력 계산을 한 트랜잭션으로 처리한다.
27. 하나의 상위 요청이 여러 퀘스트·지갑 원장을 만들 때는 원래 `Idempotency-Key`와 대상 `quest_id`로 결정적인 하위 키를 파생한다. 같은 상위 키 재시도는 같은 하위 키를 사용하고 서로 다른 퀘스트 원장이 `(wallet_id, idempotency_key)`에서 충돌하지 않게 한다.
28. 코인 퀘스트 시작 승인은 지갑·예산과 함께 `couples` 위험 상태 행을 잠그고 베팅 중지가 아닌지 재검사한다. 일반 개인 퀘스트가 `PARTNER_JUDGMENT_REJECTED`, `PARTNER_FINAL_APPROVAL_REJECTED`, `RESULT_CONFIRMATION_TIMEOUT`으로 `INVALID` 정산되면 같은 커플 행을 잠근 뒤 고유한 `couple_betting_incidents` 기록을 남긴다. `SYSTEM_ERROR`와 관계 종료에 따른 무효는 집계하지 않는다. 최근 위험 집계 구간의 세 번째 사건이면 `betting_suspension_generation + 1`, `betting_suspended_at`과 사유 갱신을 한 트랜잭션으로 처리한다.
29. `POST /reports`는 신고 행 생성과 대상 인증 자료의 `retention_reason = SAFETY_REPORT`, `hold_until`, `storage_scope = RESTRICTED_PENDING` 전환을 한 DB 트랜잭션으로 처리한다. 삭제·TTL 작업은 활성 hold를 건너뛰며, 저장소 격리 작업이 성공한 뒤에만 `RESTRICTED`로 바꾸고 원본 삭제를 허용한다.
30. 정산 규칙 확인 API는 `Idempotency-Key`를 요구하고 커플 행을 잠근 뒤 현재 `betting_suspension_generation`에 사용자별 최초 확인 시각을 기록한다. 재시도는 그 최초 시각을 바꾸지 않는다. 두 사람의 확인 시각이 모두 현재 `betting_suspended_at` 이후일 때만 중지 상태와 사유를 지우고 `betting_risk_window_started_at`을 두 번째 최초 확인 시각으로 갱신하며, 이 시각 이전 사건은 다음 집계에서 제외한다.

31. 인증 자료의 일반 보관 기한은 이벤트가 제안하는 새 기한을 `candidate_expiry`로 계산하고, 기존 `expires_at`이 `NULL`이면 새 기한을 저장하며 아니면 `LEAST(기존 expires_at, candidate_expiry)`만 저장한다. 정산의 제안값은 `settled_at + 30일`, 연결 종료의 제안값은 `ended_at + 30일`이다. 연결 종료 첫 트랜잭션은 미정산 자료까지 해당 커플의 모든 인증 자료에 종료 제안값을 먼저 적용하고, 후속 정산도 같은 `LEAST` 규칙을 사용해 기한을 다시 늘리지 못한다. 제출자 삭제 또는 선택 인증 자료 동의 철회 요청은 대상을 즉시 `BLOCKED`로 바꾸고 `deletion_requested_at = now`, `deletion_due_at = now + 7일`로 저장한다. 삭제 작업은 `expires_at`과 `deletion_due_at` 중 더 빠른 시각을 사용하며 멱등 실행한다. 활성 안전 보존이 있는 경우 일반 서비스 접근은 계속 차단하되 제한 보존본은 `hold_until`까지 유지하고, 기한 종료 후 삭제한다.
32. `POST /auth/reauth`는 현재 인증 수단을 다시 확인한 뒤 계정 탈퇴 전용·5분 만료·1회용 `reauth_grants`를 발급한다. `DELETE /users/me`는 사용자·재인증 권한·현재 탈퇴 작업을 잠그고 유효한 권한을 원자적으로 소비한다. 첫 트랜잭션에서 `users.status = DELETION_PENDING`, 모든 세션 폐기, 현재 관계의 16번 즉시 차단 처리, 사용한 `reauth_grant_id`와 `account_deletion_jobs` 생성을 함께 커밋한다. 재시도는 새 권한 소비 전에 `(user_id, idempotency_key, reauth_grant_id)`가 같은 기존 작업을 조회하고, 탈퇴 조회 전용 제한 세션과 같은 재인증 토큰 지문이 모두 확인된 경우에만 기존 결과를 반환한다. 신규 탈퇴는 권한이 미소비·미만료인 경우에만 생성한다. 후속 작업은 일반 인증 자료를 7일 이내, 그 밖의 일반 개인정보를 30일 이내 삭제·익명화하고, 안전 신고·법적 의무로 분리 보존하는 최소 정보는 각 `hold_until`을 적용한다.

33. 퀘스트 응답 DTO는 호출자 역할과 상태별로 직렬화한다. 파트너에게 `PENDING_APPROVAL`은 현재 제출 버전, `CHANGE_REQUESTED`는 마지막 제출 버전, `ACTIVE` 이후는 승인된 버전을 기준으로 제목·성공 조건·난이도·건 코인·예상 성공 보너스·수행 및 결과 시각·최소 수행 시간·승인 마감·인증 방식·현재 버전 ID와 번호·서버 계산 변경 항목을 보여준다. `ACTIVE` 이후에는 시작 승인 때 고정된 `predictedResult`도 두 사람에게 반환한다. 회수 상태에서는 이미 전달된 불변 버전과 응답 전용 `auditProjectionStatus = RECALLED`만 허용한다. 작성자의 전체 잔액·잠긴 잔액·지갑 원장·뽑기·구매 내역·남은 개인 예산·제출 전 초안은 반환하지 않는다. `GET /wallet`, 지갑 원장과 작성자 전용 승인 견적은 본인에게만 허용한다.
34. P2 중복 선택은 요청의 `expectedStatus = PENDING`을 요구하고 뽑기 행·개별 단위·선택 행을 함께 잠근다. PENDING 행에는 아직 알 수 없는 요청 멱등 키를 저장하지 않고, 24시간 안의 최초 사용자 선택을 커밋할 때 그 요청의 키를 기록한다. `KEEP_FOR_EXCHANGE`는 단위를 `AVAILABLE`로, `CONVERT_TO_FRAGMENTS` 또는 만료 작업은 단위를 `CONVERTED`로 바꾸고 조각을 한 번만 지급한다. 선택 재시도는 같은 결과를 반환하고, 만료와 사용자 선택이 경합해도 둘 중 하나만 커밋한다.
35. P2 교환 생성·수정·확정은 커플, 현재 예약 주와 필요 시 완료 주의 교환 예산, 제안, 현재 불변 제안 버전, 현재 버전에 실제로 들어온 개별 단위와 활성 예약을 정해진 순서로 잠근다. 각 사용자는 자기 단위만 선택·예약할 수 있고 최초 생성자가 파트너 단위를 지정하거나 잠글 수 없다. 파트너 단위의 정의는 현재 버전의 `requested_item_definition_id`와 같아야 한다. 연결 7일·현재 연결·주 3회·양쪽 여분 1개 유지·등급 또는 교환 가치·양도 가능 상태를 매번 서버에서 재검사한다. 자기 단위 추가·교체는 상대 단위를 새 버전에 복사하고 본인의 이전 예약을 `RELEASED`로 바꾼 뒤 새 단위를 예약하며, 이전 확인을 무효화하되 최초 `expires_at`은 연장하지 않는다. 완료 주가 예약 주와 다르면 예약 슬롯을 완료 주로 원자 이동한다. 완료 주 한도가 찼으면 마지막 확인과 완료를 롤백하고 사용자가 만료 전 재시도하게 한다. 같은 현재 버전에 양쪽 단위가 모두 있고 양쪽이 확인한 경우에만 ACTIVE 예약을 `CONSUMED`, 주간 수를 `reserved → completed`로 바꾸고 두 단위의 소유자를 맞바꿔 `AVAILABLE + UNBOUND`로 저장하며 이전 이벤트를 추가한다. 취소·거절·24시간 만료·연결 종료·차단은 예약과 주간 예약 수를 멱등하게 해제한다.
36. P2 선물은 `item_gifts.row_version`, 수령자의 `user_interaction_settings.row_version`, 현재 연결과 수신 설정을 생성·수락 때 확인한다. `gift_duplicate_policy = REJECT_IF_OWNED`이면 상점·보유 아이템 선물 모두 수령자의 같은 정의 보유 여부를 검사한다. 상점 선물은 현재 활성 불변 가격 버전, 전투력 0·유효 꾸미기 슬롯을 검증하고 코인을 잠근 뒤, 수락 시 생성 때의 가격 스냅샷대로 소각하며 수령자 `AVAILABLE + TRANSFER_LOCKED` 꾸미기 단위를 생성한다. 가격·아이템·시행 시각은 불변이고 사용된 버전도 `ACTIVE → RETIRED` 상태 변경만 허용한다. 보유 아이템 선물은 현재 예약 주와 필요 시 완료 주 예산, 보내는 사람의 주 1회 한도와 허용된 개별 단위를 잠근다. 완료 주 한도가 찼으면 수락 전체를 롤백한다. 수락 시 ACTIVE 예약을 `CONSUMED`, 주간 수를 `reserved → completed`로 바꾸고 같은 단위의 소유자를 바꿔 `AVAILABLE + TRANSFER_LOCKED`로 저장한다. `TRANSFER_LOCKED` 단위는 조각 변환·재선물·재교환할 수 없다. 수신 설정을 끄는 트랜잭션은 요청의 `Idempotency-Key`를 정리 작업에 기록하고 당시 PENDING 선물을 `gift_settings_cleanup_items`로 스냅샷한 뒤 작업 ID를 반환한다. 같은 키 재시도는 기존 작업을 반환한다. 작업이 각 선물을 `REJECTED_SETTINGS_DISABLED`로 바꾸며 코인·단위 반환을 한 번만 처리하고, 모든 자원이 끝나야 상위 작업을 완료한다. 거절·72시간 만료·연결 종료·차단도 코인 또는 단위 예약을 한 번만 반환하고, 완료된 보유 아이템 선물 횟수는 되돌리지 않는다.
37. P2 정식 레이드는 보스 버전을 편성하기 전에 `max_hp > 0`, 난이도, 전투 설정, 에셋·스킬·보상·연출 데이터의 스키마와 참조를 검증한다. 순환 1~3번 슬롯은 `NORMAL`, 4번 슬롯은 `SEASONAL` 보스만 허용한다. 매주 월요일 생성 시 `raid_rotation_weeks`의 `boss_definition_version_id`를 `weekly_raid_instances`에 복사하고 주차+버전 복합 FK로 원본 편성을 고정한다. 생성 후 운영 데이터가 바뀌어도 진행 중 인스턴스의 HP·난이도·전투 설정·외형·스킬·보상·연출 스냅샷은 변경하지 않는다.
38. P2 꾸미기 적용·해제는 `expectedSlotRowVersion`과 `expectedCurrentItemUnitId`를 요구하고 사용자·슬롯·개별 단위를 잠근다. 두 예상값, 소유권, `COSMETIC`, 정의의 슬롯, `AVAILABLE` 상태가 모두 맞을 때만 처리한다. 새 단위를 적용하면 기존 슬롯 단위를 `AVAILABLE`, 새 단위를 `EQUIPPED`로 바꾸고 슬롯 행의 `row_version`을 올린다. 해제는 행을 지우지 않고 단위를 `AVAILABLE`, 슬롯 참조를 NULL로 바꾸며 버전을 올린다. `TRANSFER_LOCKED`는 적용을 막지 않지만 교환·선물·조각 변환은 계속 금지하며, 꾸미기 능력치는 항상 0으로 서버가 검증한다.

---

# 32. 주요 API 구조 예시

> 결정 상태: `[확정]` · 출시 범위: `[MVP]`

```text
POST /auth/login
POST /auth/logout
POST /auth/reauth
GET  /policy-versions/current
POST /onboarding/age-eligibility
PUT  /consents/{policyType}

POST /couples/invites
DELETE /couples/invites/{inviteId}
POST /couples/invites/preview
POST /couples/join
POST /couples/pending/{inviteId}/confirm
POST /couples/pending/{inviteId}/reject
GET  /couples/me
POST /couples/me/end
POST /couples/me/settlement-rules/acknowledge

GET  /home
GET  /actions/pending
PATCH /notification-settings
POST /push/devices
DELETE /push/devices/{deviceId}

POST /monsters/starter
GET  /monsters/me
GET  /monsters/partner
PATCH /monsters/me/name

POST /quests
PATCH /quests/{questId}/draft
DELETE /quests/{questId}/draft
POST /quests/{questId}/submit
POST /quests/{questId}/recall
GET  /quests
GET  /quests/{questId}
GET  /quests/{questId}/versions
GET  /quests/{questId}/approval-quote

POST /quests/{questId}/approve
POST /quests/{questId}/request-change
POST /quests/{questId}/reject
POST /quests/{questId}/cancel
POST /quests/{questId}/confirm-cancel
POST /quests/{questId}/reject-cancel

PUT  /quests/{questId}/partner-result
POST /quests/{questId}/partner-result/reject
POST /quests/{questId}/partner-result/final-approve
GET  /quests/{questId}/settlement

GET  /rescue-quest
POST /rescue-quest/complete

GET  /wallet
GET  /wallet/transactions

GET  /gacha/probabilities
GET  /gacha/pity
POST /gacha/draw
GET  /gacha/history

GET  /inventory
POST /inventory/item-units/{itemUnitId}/equip
POST /inventory/item-units/{itemUnitId}/unequip

GET  /coop-boss/weekly
POST /coop-boss/weekly/participate

POST   /quests/{questId}/evidence/upload-url
POST   /evidence/{evidenceId}/complete
POST   /quests/{questId}/evidence/submit
GET    /evidence/{evidenceId}/content
DELETE /evidence/{evidenceId}

POST   /users/{userId}/block
DELETE /users/{userId}/block
POST   /reports
GET    /users/me/export
DELETE /users/me
GET    /users/me/deletion
DELETE /consents/{policyType}
```

초대 생성·참여·최종 확인, 퀘스트 제출·회수·폐기·수정 요청·거절·시작 승인·취소·인증 자료 제출·파트너 실제 결과 선택·판정 거절·최종 승인·정산, 구조 퀘스트·뽑기·장비 장착·해제·간이 협동 참여와 `POST /couples/me/end`, `POST /couples/me/settlement-rules/acknowledge`, `POST /users/{userId}/block`, `POST /reports`, `DELETE /users/me`는 `Idempotency-Key`를 요구한다. 서버는 재요청 시 이미 커밋된 동일 상태와 최초 처리 시각·정산 결과를 반환하며 추가 원장·환불·알림을 만들지 않는다. 초안 `PATCH`는 `expectedRowVersion`, 시작 승인·수정 요청·거절은 `questVersionId`와 `expectedRowVersion`을 요구한다.

`POST /quests`는 작성자만 볼 수 있는 `DRAFT`를 만들고 어떤 자원도 잠그지 않는다. `PATCH /quests/{questId}/draft`는 `expectedRowVersion`을 요구하고 `DRAFT` 또는 `CHANGE_REQUESTED`에서만 수정하며, `PENDING_APPROVAL`이면 먼저 회수해야 한다. `DELETE /quests/{questId}/draft`는 같은 두 편집 상태에서만 `Idempotency-Key`와 `expectedRowVersion`을 받아 작업 초안을 지우고 `DISCARDED`로 끝낸다. 한 번도 제출하지 않은 폐기 초안은 파트너에게 계속 숨기고, 전달 이력이 있으면 파트너에게 과거 불변 버전과 `DISCARDED` 감사 상태만 유지한다. `POST /quests/{questId}/submit`은 `expectedRowVersion`을 요구하며, 도전자의 튜토리얼이 아직 최종 종료되지 않았으면 `TUTORIAL_REQUIRED`를 반환한다. 서버 계산 승인 마감이 이미 지났으면 새 버전 없이 `APPROVAL_DEADLINE_NOT_FUTURE`를 반환하고 편집 상태를 유지한다. 직전 제출본과 변경점이 없으면 `UNCHANGED_QUEST_VERSION`을 반환한다. 유효한 경우에만 새 불변 버전을 만들고 `questVersionId`, `versionNo`, `approvalDeadlineAt`, `changedFields`, 새 `rowVersion`을 반환한다. 작성자만 `questVersionId + expectedRowVersion`으로 `POST /quests/{questId}/recall`을 호출할 수 있다. 파트너의 수정 요청·거절·시작 승인도 같은 두 필드를 요구하고 요청 문구와 대상 버전을 기록하며, 시작 승인은 `predictedResult`도 필수로 받는다. 이 네 가지 승인 대기 변이는 행 잠금 뒤 마감을 먼저 검사해 마감 도달 시 `APPROVAL_EXPIRED`가 우선한다. 모든 성공 변경 응답은 갱신된 `rowVersion`을 반환한다.

`GET /quests`, `GET /quests/{questId}`, `GET /quests/{questId}/versions`와 `GET /home`, `GET /actions/pending`의 **퀘스트 항목**은 모두 같은 상태·역할 투영기를 사용하고 각 퀘스트의 현재 `rowVersion`을 반환한다. 새 `DRAFT`는 작성자에게만 보인다. 회수된 퀘스트는 파트너의 활성 목록에서 제거하고, 파트너에게 이미 전달됐던 불변 버전과 응답 전용 `auditProjectionStatus = RECALLED`만 다시 볼 수 있게 하며 현재 작업 초안은 숨긴다. `RECALLED`는 DB 퀘스트 상태가 아니다. `CHANGE_REQUESTED`는 마지막 제출 버전과 파트너의 수정 요청만, `PENDING_APPROVAL`은 현재 `questVersionId`와 제출 버전만, `ACTIVE` 이후는 `approvedQuestVersionId`의 내용을 기준으로 반환한다. 작성자만 현재 `quest_drafts`를 받을 수 있다.

파트너용 퀘스트 응답에는 현재 허용된 버전의 제목·성공 조건·난이도·건 코인·예상 성공 보너스·수행 및 결과 시각·최소 수행 시간·승인 마감·인증 방식·현재 버전 ID와 번호·서버 계산 변경 항목을 포함한다. `ACTIVE` 이후에는 시작 승인 때 고정된 `predictedResult`도 두 사람에게 반환한다. 전체 가용·잠긴 잔액, 전체 지갑 원장, 뽑기·구매 내역, 남은 개인 코인 퀘스트 횟수·보너스 한도와 제출 전 초안은 포함하지 않는다. `GET /quests/{questId}/approval-quote`는 작성자 전용이며 현재 초안 또는 제출본의 허용 베팅 범위와 본인의 남은 개인 한도만 반환한다.

`POST /quests/{questId}/approve`는 `{ questVersionId, expectedRowVersion, predictedResult }`를 받고, 요청한 버전이 현재 버전과 같고 `predictedResult`가 `SUCCESS` 또는 `FAILURE`이며 서버 시각이 `approvalDeadlineAt`보다 빠를 때만 시작 승인한다. 도전자의 `tutorial_completed_at`이 없으면 자원 변화 없이 `TUTORIAL_REQUIRED`를 반환한다. 응답에는 `status`, `approvedQuestVersionId`, `predictedResult`, 새 `rowVersion`, `budgetDateKst`, 해당 퀘스트의 `lockedStake`, `rewardReservedAmount`, `dueAt`, `resultAt`, `resultConfirmationDeadlineAt`만 포함하며 작성자의 남은 개인 예산은 파트너에게 반환하지 않는다. 마감과 같거나 늦으면 자원 변화 없이 `APPROVAL_EXPIRED`와 갱신된 `rowVersion`을 반환한다. 승인 시 잔액이나 한도가 부족하면 전체를 롤백하고 파트너에게는 금액 없는 `QUEST_REQUIRES_REVISION`, 작성자 개인 알림에는 조정 가능한 범위를 제공한다. 서버는 템플릿 퀘스트의 `templateId`로 난이도와 최소 수행 시간을 고정하고, `sourceType = CUSTOM`이면 표시 난이도와 무관하게 베팅금을 최대 100C, 성공 보너스율을 50%로 제한한다.

`PUT /quests/{questId}/partner-result`는 파트너만 `{ selectedResult, expectedRowVersion }`을 보내 호출한다. `AWAITING_RESULT`에서는 최초 선택을 저장하고 `PENDING_FINAL_APPROVAL`로 전환하며, `PENDING_FINAL_APPROVAL`에서는 최종 승인 전 선택값을 수정한다. 두 경우 모두 `resultConfirmationDeadlineAt` 이전이어야 하고 새 `selectionRevision`, `selectedResult`, `status`, `rowVersion`을 반환한다. 도전자는 호출할 수 없으며 조회 응답에는 확인 대기 상태만 제공한다.

`POST /quests/{questId}/partner-result/reject`는 파트너가 `{ expectedRowVersion }`으로 호출한다. `AWAITING_RESULT`에서 호출하면 `PARTNER_JUDGMENT_REJECTED`, `PENDING_FINAL_APPROVAL`에서 호출하면 `PARTNER_FINAL_APPROVAL_REJECTED`로 `INVALID` 정산하고 잠긴 원금만 반환한다. `POST /quests/{questId}/partner-result/final-approve`는 `PENDING_FINAL_APPROVAL`에서 파트너가 `{ selectedResult, selectionRevision, expectedRowVersion }`을 보내 현재 작업 값과 일치할 때만 호출할 수 있다. 서버는 선택값에 따라 `SUCCESS` 또는 `FAILURE` 정산을 한 번 커밋하고 같은 요청 재시도에는 최초 정산을 반환한다. 마감 스케줄러는 최종 승인이 없는 두 대기 상태를 `RESULT_CONFIRMATION_TIMEOUT`의 `INVALID`로 한 번만 정산한다.

`GET /gacha/probabilities`는 각 뽑기의 `probabilityVersionId`를 반환한다. `POST /gacha/draw`는 사용자가 확인한 `expectedProbabilityVersionId`를 본문에 포함해야 하며 현재 활성 버전과 다르면 코인·뽑기권을 차감하지 않고 `409 Conflict`와 새 버전 ID를 반환한다. 성공한 `gacha_draws.probability_version_id`는 요청에서 확인한 버전과 같아야 한다.

`POST /couples/join`은 초대를 `PENDING_CONFIRMATION`으로 전환하고 `inviteId`를 반환한다. 두 번째 `POST /couples/pending/{inviteId}/confirm`에서 양쪽 확인과 안전 조건을 재검사한 후에만 연결을 생성한다.

`POST /couples/me/end`는 관계 접근 차단을 커밋한 뒤 `relationshipStatus = ENDED`, `relationshipCleanupStatus = PROCESSING`, `relationshipEndJobId`, `targetResourceCount`, `processedResourceCount`를 즉시 반환한다. 재호출은 같은 작업을 반환한다. `GET /couples/me`는 같은 관계 정리 상태와 대상·처리 수를 제공하고, 모든 스냅샷 자원이 끝난 `COMPLETED`에서 최종 사용 가능·잠긴 잔액을 함께 반환한다.

`POST /quests/{questId}/evidence/upload-url`은 `questId`에 연결된 임시 업로드를 만든다. 업로드 뒤 `POST /evidence/{evidenceId}/complete`가 검사·재인코딩을 시작하며, `processingStatus = READY`인 처리본만 `GET /evidence/{evidenceId}/content`가 권한 검사 후 `no-store`로 전달한다.

업로드 URL 발급은 활성 선택 동의, 현재 커플 권한, 허용 퀘스트 상태, 장당 10MB·퀘스트당 3장·총 20MB 한도를 확인한다. 도전자의 `POST /quests/{questId}/evidence/submit`은 같은 퀘스트·본인 소유·`READY`인 `evidenceIds`만 받아 `submittedForReviewAt`을 한 번 기록하고 파트너 결과 확인 화면에 공개한다. 완료되지 않았거나 검토용으로 제출되지 않은 임시 업로드는 서버 TTL 작업이 삭제한다.

`DELETE /evidence/{evidenceId}`는 제출자만 호출할 수 있고 대상을 즉시 `BLOCKED`로 바꾸며 `202 Accepted`, `accessBlockedAt`, `ordinaryDeletionDueAt`을 반환한다. 일반 저장본은 7일 이내 삭제하고, 활성 신고에 따른 제한 보존본은 신고 유무를 상대방에게 노출하지 않은 채 27.2절의 예외 기한을 적용한다.

신규 사용자의 `POST /auth/login`과 `GET /policy-versions/current`는 로케일별 현재 `ACTIVE` 정책의 `policyVersionId`, 버전, 필수 여부와 시행 시각을 반환하고, 로그인 응답은 `requiredPolicyVersionIds`와 `missingPolicyVersionIds`를 함께 제공한다. `POST /onboarding/age-eligibility`로 만 14세 이상 여부를 확인한다. `PUT /consents/{policyType}`는 `policyVersionId`를 요구하고, 경로의 정책 유형·요청 로케일과 일치하는 현재 `ACTIVE` 버전인지 검증한 뒤 동의를 기록한다. 연령과 현재 필수 버전을 모두 통과한 뒤 일반 세션으로 전환한다. 연령 미달 또는 확인 거절 응답에는 가입 불가 결과를 반환하고 해당 세션을 즉시 폐기한다. 필수 동의가 없거나 철회된 세션에서는 내보내기·탈퇴 외 일반 API를 허용하지 않는다.

`DELETE /consents/EVIDENCE_OPTIONAL`은 신규 업로드를 즉시 막고 사용자가 제출한 기존 일반 인증 자료를 모두 `BLOCKED`로 바꾸어 7일 이내 삭제 대기열에 넣는다. 필수 약관·개인정보 동의 철회는 내보내기 안내와 `DELETE /users/me` 흐름으로 연결한다.

`POST /auth/reauth`는 현재 로그인 수단을 다시 확인하고 원문을 저장하지 않는 5분 만료·1회용 `reauthToken`을 반환한다. `DELETE /users/me`는 `Idempotency-Key`와 `X-Reauth-Token`을 모두 요구하고, 재인증 권한을 소비한 뒤 `202 Accepted`, `deletionRequestId`, `evidenceDeleteBy`, `personalDataDeleteBy`를 반환한다. 탈퇴 첫 커밋 후에는 모든 기존 세션을 폐기한다. 응답을 받지 못한 경우 `DELETION_PENDING` 계정의 재로그인은 탈퇴 조회 전용 제한 세션만 발급하고 `GET /users/me/deletion`에서 같은 신청 ID·기한·상태를 반환한다.

기기 푸시 토큰은 전송에 필요한 원문을 암호화해 저장하고 비교용 지문만 고유 키로 사용한다. `PATCH /notification-settings`은 잠금 화면 상세 미리보기를 기본 `false`로 유지하며, OS에서 푸시를 끈 경우에도 인앱 대기함은 항상 제공한다.

`POST /monsters/starter`는 `{ species, name }`을 받아 로그인 사용자의 스타팅 배츄를 한 번만 생성한다. 커플 연결 상태가 `CONNECTED`일 때만 호출할 수 있고, `species`는 `STARLIGHT`, `WAVE`, `SUNSET`, `FOREST` 중 하나여야 한다. 성공 응답은 `growthStage = EGG`, `recognizedSuccessCount = 0`이며 같은 트랜잭션에서 계정 성장 행과 활성 배츄 참조를 생성한다. 이미 스타팅 배츄가 있으면 `409 Conflict`를 반환한다. `PATCH /monsters/me/name`은 `{ name }`을 받아 본인 활성 배츄의 이름을 변경한다. MVP에서는 별도 변경권 없이 무료로 변경할 수 있으며, 두 API 모두 4절의 배츄 이름 규칙을 서버에서 검증한다.

`GET /home`, `GET /monsters/me`, `GET /monsters/partner`의 몬스터 객체는 다음 필드를 같은 의미로 제공한다.

```text
id, name, species, growthStage
recognizedSuccessCount, nextGrowthMilestone, reachedMilestones
masteryRewards
accountLevel, currentLevelExp, nextLevelRequiredExp
baseHp, baseAttack
equipmentBonusHp, equipmentBonusAttack
finalHp, finalAttack, combatPower
```

`accountLevel`, 경험치와 기본 능력치는 `user_progressions`에서 가져오는 계정 공통 값이고 `growthStage`와 `recognizedSuccessCount`만 배츄별 값이다. `nextGrowthMilestone`은 현재 상태에 따라 `HATCH`, `20`, `40`, `60`, `80` 또는 `null`이며, 레벨이 올라도 외형 단계는 바뀌지 않는다. `GET /quests/{questId}/settlement`은 같은 정산 재조회에서도 `creditedMonsterId`, `recognizedSuccessBefore`, `recognizedSuccessAfter`, `growthStageBefore`, `growthStageAfter`, `unlockedMilestones`, `grantedRewards`를 동일하게 반환한다.

## 후속 P2 API

> 결정 상태: `[확정]` · 출시 범위: `[후속]` · 우선순위: `P2`

```text
GET   /monsters
PATCH /monsters/{monsterId}/activate
PATCH /monsters/{monsterId}/name
GET   /monsters/egg-choice-offers
POST  /monsters/egg-choice-offers/{offerId}/select

POST /inventory/item-units/{itemUnitId}/disposition

GET  /item-exchanges
POST /item-exchanges
PUT  /item-exchanges/{exchangeId}/my-item
POST /item-exchanges/{exchangeId}/confirm
POST /item-exchanges/{exchangeId}/reject
POST /item-exchanges/{exchangeId}/cancel

GET  /shop/cosmetic-offerings
GET  /cosmetic-loadout
PUT  /cosmetic-loadout/{cosmeticSlot}
DELETE /cosmetic-loadout/{cosmeticSlot}
GET  /item-gifts
POST /item-gifts/shop
POST /item-gifts/inventory
POST /item-gifts/{giftId}/accept
POST /item-gifts/{giftId}/reject
POST /item-gifts/{giftId}/cancel
GET   /interaction-settings
PATCH /interaction-settings
GET   /interaction-settings/gift-cleanup/{jobId}

GET /raids/rotation
GET /raids/weekly
```

P2 출시 시 소급 작업을 실행한 뒤 상시 outbox 작업자가 `SUCCESS_80` 마일스톤은 있지만 선택권이 없는 기록을 계속 찾아 사용자당·마일스톤당 `egg_choice_offer`를 정확히 1개 만든다. 새 종이 추가되면 `PENDING_CONTENT` 선택권도 재평가해 후보 버전을 올리고 `PENDING_SELECTION`으로 바꾼다. `POST /monsters/egg-choice-offers/{offerId}/select`는 `{ species, name, expectedOptionsVersion }`과 `Idempotency-Key`를 요구한다. 서버는 선택권 행과 사용자의 배츄를 잠그고 현재 후보·이름 규칙·미보유 종 여부를 다시 검사한 뒤, `sourceEggChoiceOfferId`가 고유한 새 배츄를 `EGG`, 인정 성공 0회로 한 번만 생성한다. 다른 선택권으로 해당 종을 먼저 얻어 후보가 오래됐으면 생성하지 않는다. 미보유 종이 1개 이상 남으면 후보 버전을 갱신하고 `409 STALE_EGG_CHOICE_OPTIONS`와 새 버전을 반환하며, 하나도 남지 않으면 선택권을 `PENDING_CONTENT`로 바꾸고 같은 409 응답에 그 상태를 반환한다. 새 배츄는 활성화 후 첫 인정 성공 정산에서 `HATCH` 마일스톤과 함께 `BABY`로 부화한다. 활성 배츄 교체는 `user_active_monsters`를 잠그고 소유권을 확인하며, 이미 시작된 정산의 `creditedMonsterId`를 뒤바꾸지 않는다.

교환 최초 생성자는 `ownItemUnitId`와 `requestedItemDefinitionId`로 자기 적격 단위를 보내고 원하는 상대 아이템 정의를 요청한다. 파트너도 자기 적격 목록에서 요청 정의와 같은 자기 단위만 추가·교체할 수 있으며 이때 새 `offerVersion`이 생성되고 양쪽 확인이 초기화된다. 교환 변경은 `expectedRowVersion + offerVersion`, 선물 변경은 `expectedRowVersion`, 수신 설정 변경은 설정의 `expectedRowVersion`, 중복 선택은 `expectedStatus = PENDING`을 요구한다. 모든 변경 요청은 `Idempotency-Key`도 요구하고, 임의 `recipientId` 대신 현재 커플 상대를 서버가 결정한다. 성공한 교환·선물 변경 응답은 새 `rowVersion`과 현재 `offerVersion` 또는 상태를 반환한다. 주간 완료 한도 오류는 마지막 확인·수락을 저장하지 않고 현재 버전을 그대로 반환한다. 상점 응답과 정식 레이드 응답은 각각 불변 `shopOfferingVersionId`, `bossDefinitionVersionId`를 포함한다. `PATCH /interaction-settings`로 선물 수신을 끄면 기존 대기 선물은 수락 불가가 되고 `giftSettingsCleanupJobId`, 대상·완료 수를 응답한다. `GET /interaction-settings`에서 현재 값과 `rowVersion`, 정리 중 여부를 조회하고 작업 상세 API에서 반환 진행도를 확인한다. 정식 레이드의 턴 행동 API는 20절의 제안 전투 방식이 확정된 뒤 별도 계약으로 추가한다.

P2에서 `GET /actions/pending`은 알 선택, 중복 처리 선택, 상대 아이템 추가, 교환 최종 확인, 선물 수락·거절과 각 만료 시각도 반환한다. 퀘스트가 아닌 항목은 자기 엔터티의 `status`와 동시성 기준인 `rowVersion`, `offerVersion`, `optionsVersion` 또는 `slotRowVersion` 중 해당 값을 반환하며 퀘스트 `rowVersion`을 대신 사용하지 않는다. 알 선택, 상점·보유 아이템 선물 생성, 수락·거절·취소, 중복 처리 선택, 교환 생성·자기 단위 변경·확인·거절·취소, 꾸미기 적용·해제와 활성 배츄 교체는 모두 `Idempotency-Key`를 요구한다. `GET /cosmetic-loadout`은 슬롯별 단위·에셋·`slotRowVersion`을 반환하고 적용·해제 API는 `expectedSlotRowVersion`, `expectedCurrentItemUnitId`, 새 단위의 `expectedItemUnitState`를 검사한다.

P2에서 중복이 나온 `POST /gacha/draw` 성공 응답은 `itemUnitId`, `duplicateDisposition = PENDING`, `dispositionExpiresAt`을 함께 반환해 즉시 보관·조각 선택 화면을 열 수 있게 한다. 상점 선물 생성은 사용자가 본 `expectedShopOfferingVersionId`가 현재 활성 버전과 다르면 코인을 잠그지 않고 새 버전을 반환한다. 이미 생성된 대기 선물은 이후 활성 버전이 바뀌어도 저장된 가격 스냅샷을 사용한다.

뽑기·인벤토리·장비 응답에서 아이템별 `powerContribution`이 필요하면 서버가 `effectiveHpBonus × 5 + effectiveAttackBonus × 20`으로 계산해 읽기 전용 값으로 제공한다. MVP의 유효 보너스는 아이템 정의의 기본 보너스와 같다.

MVP에서는 `home` API 하나에서 다음 정보를 한 번에 내려주는 것이 편하다.

```text
내 몬스터
파트너 몬스터
사용 가능 코인과 잠긴 코인
진행 중 퀘스트
승인 대기 퀘스트
결과 확인 대기 퀘스트
실제 결과 선택·최종 승인·마감 포함 처리 대기 항목
파트너 전투력·연승·주간 성장 비교
케미 게이지
이번 주 간이 협동 보스
```

`home`의 파트너 비교 객체는 각 사용자의 `weeklyCombatPowerDelta`를 제공한다. 케미 게이지는 `current`, `target`, `unlocked`, `coopBossPlayable`, `weekStartAt`, `weekEndAt`을 제공한다. `target`은 `10`이고, 시간 값은 KST 주간 경계를 기준으로 한다. 간이 협동 보스 객체는 `requiredPower`, 양쪽 `participated`, `combinedPowerSnapshot`, `status`와 보상 수령 상태를 제공한다.

---

# 33. 개발 단계 제안

> 결정 상태: `[제안]` · 출시 범위: 단계별 표기

## 1단계: 기본 연결

> 출시 범위: `[MVP]` · 우선순위: `P0`

* 로그인
* 만 14세 이상 확인과 정책 동의 버전
* 커플 초대 코드 만료·철회·미리보기·일회 사용
* 연결 종료·차단·신고·탈퇴
* 스타팅 몬스터 선택·이름 설정
* 기본 프로필
* 홈 화면

## 2단계: 퀘스트와 코인

> 출시 범위: `[MVP]` · 우선순위: `P0`

* 작성자 전용 초안과 제출 버전 생성
* 승인 전 회수·수정 요청·새 버전 재제출·거절
* 최소 수행 시간 기반 승인 마감과 승인 후 불변 처리
* 난이도별 베팅 상한과 일일 보너스 예약
* 사용 가능·잠긴 코인과 원장 트랜잭션
* 파트너 실제 결과 선택·수정과 최종 승인
* 24시간 최종 승인 마감과 미승인 `INVALID` 자동 정산
* 구조 퀘스트
* 거래성 푸시와 인앱 대기함
* 비공개 인증 자료 업로드·권한·삭제
* 멱등성·동시 승인·중복 정산 테스트

## 3단계: 몬스터 성장

> 출시 범위: `[MVP]` · 우선순위: `P0`

* 경험치
* 레벨
* 연승
* 능력치
* 전투력 계산
* 튜토리얼 성공 또는 모든 활성 `EGG` 배츄의 첫 인정 성공 시 `HATCH` 기록과 부화
* 인정 성공 20회 중간 성장·40회 최종 진화
* 인정 성공 60회 종별 오라·칭호·전투력 0 숙련 장신구 지급
* 인정 성공 80회 마일스톤 기록과 후속 선택권 소급 기반
* 성장 단계·마일스톤 동시 정산·임계값 건너뜀·재시도 테스트
* 단계별 정적 이미지 반영

## 4단계: 뽑기와 장비

> 출시 범위: `[MVP]` · 우선순위: `P0`

* 세 종류의 뽑기
* 불변 확률 버전과 천장
* 인벤토리
* 중복 조각
* 장비 장착
* 지갑·뽑기·천장·인벤토리 원자성 테스트

## 5단계: MVP 경쟁·협동 검증

> 출시 범위: `[MVP]` · 우선순위: `P0`

* 파트너 전투력·연승·주간 성장 비교
* 케미 게이지
* 간이 협동 보스
* 양쪽 참여와 동일 보상

## 6단계: 출시 품질

> 출시 범위: 출시 차단 품질은 `[MVP]` `P0`, 선택형 개선은 `[후속]` `P1`

* `[MVP P0]` 기본 퀘스트 템플릿 20개
* `[MVP P0]` 스크린리더·글자 확대·명암비·모션 감소 점검
* `[MVP P0]` 연결 종료·인증 자료 접근 차단·데이터 삭제 테스트
* `[MVP P0]` 코인 수급 시뮬레이션과 잔액 분포 점검
* `[후속 P1]` 선택형 출석·이벤트·홍보 알림
* `[후속 P1]` 성공·실패 애니메이션
* `[후속 P1]` 도감과 통계

## 7단계: 확장

> 출시 범위: `[후속]` · 우선순위: 본문의 P1·P2 표기를 따름

* PvP
* 선택형 정식 커플 레이드
* 일반 보스 3종·시즌 보스 1종 4주 순환
* 분기 진화·추가 진화 형태
* 고급 진화 애니메이션
* 시즌
* 복수 배츄 보관함·80회 알 선택·활성 배츄 교체
* 교환용 중복 보관·중복 아이템 1:1 교환
* 귀속 꾸미기 상점 선물·제한적 보유 아이템 선물
* 꾸미기
* 공유 카드

---

# 34. 서비스 성과 지표

> 결정 상태: `[확정]` · 출시 범위: `[MVP]`

BETCHU는 다운로드 수보다 **커플 두 명이 함께 행동하는지**가 중요하다.

## MVP 지표

| 지표 | 의미 |
| --- | --- |
| 커플 연결 완료율 | 초대 미리보기 후 실제 연결했는가 |
| 첫 퀘스트 생성·정산률 | 연결 후 핵심 루프를 끝까지 완료했는가 |
| 주간 커플당 유효 정산 퀘스트 | 실제 게임 반복 횟수 |
| 시작 승인 평균 소요 시간 | 파트너가 빠르게 퀘스트 시작에 동의하는가 |
| 결과 확인 후 최종 승인 평균 소요 시간 | 파트너 판정이 정산까지 원활히 이어지는가 |
| 최종 승인 미응답률 | 결과 확인 기한 때문에 정산이 막히는가 |
| `INVALID` 발생률·사유별 비율 | 판정 거절·최종 거절·기한 만료 흐름이 과도하지 않은가 |
| 케미 게이지 달성률 | 공동 목표가 반복 행동을 만드는가 |
| 해금 커플 중 간이 보스 양쪽 참여율 | 두 사람이 실제 협동 기능에 들어오는가 |
| 간이 보스 클리어율 | 장비·전투력 성장이 작동하는가 |
| 비교 카드 확인 후 24시간 내 퀘스트 생성률 | 무보상 경쟁이 행동을 유도하는가 |
| 코인 발행액 ÷ 소비·소각액 | 경제가 장기적으로 팽창하거나 고갈되지 않는가 |
| 30일·90일 잔액 중앙값·상위 10% | 사용자별 코인 분포가 안정적인가 |
| 구조 퀘스트 사용률 | 파산 복구가 필요 이상으로 반복되는가 |
| 7일 후 재방문 커플 비율 | 반복 재미가 있는가 |

초기 두 사람 테스트에서는 다음을 확인하면 된다.

```text
일주일 동안
- 각자 퀘스트 5개 이상 완료
- 뽑기 2회 이상 진행
- 최종 승인 기한 만료 `INVALID` 20% 이하
- 파트너 판정·최종 승인 거절 `INVALID` 10% 이하
- 케미 해금 커플은 두 사람 모두 간이 보스 참여
- 다음 주에도 새 퀘스트 생성
```

## 후속 기능 지표

> 결정 상태: `[제안]` · 출시 범위: `[후속]`

| 지표 | 적용 기능 |
| --- | --- |
| 정식 레이드 참여율·클리어율 | 선택형 정식 레이드 |
| PvP 참여율·재대결률 | PvP |
| 시즌 참여율·보상 수령률 | 월간 시즌 |
| 80회 달성 후 알 선택 완료율·활성 교체율 | 복수 배츄 보관함 |
| 중복 보관 선택률·교환 제안 대비 완료율 | 중복 처리·아이템 교환 |
| 선물 생성 대비 수락률·수신 거부율 | 아이템 선물 |
| 4주차별 참여율·보스별 클리어율 | 정식 레이드 순환 |

---

# 35. BETCHU의 핵심 정체성

> 결정 상태: `[제안]` · 출시 범위: `[후속]` 장기 비전

BETCHU는 단순히 코인을 걸고 성공 여부를 확인하는 앱이 아니다.

처음에는 다음과 같은 사소한 퀘스트로 시작한다.

```text
오늘 운동하기
오늘 1시 전에 자기
서로 편한 시간에 안부 한 번 나누기
방 정리하기
```

하지만 시간이 지나면 그 행동이 전부 게임 기록으로 바뀐다.

```text
LV.43 배츄
완료한 퀘스트 384개
최고 연승 17회
전설 아이템 7개
커플 레이드 28회
함께 플레이한 기간 137일
```

결국 사용자가 얻게 되는 것은 강한 몬스터 하나가 아니라,

> **우리가 서로 어떤 약속을 했고, 얼마나 지켰으며, 그 과정에서 무엇을 함께 키웠는지 보여주는 둘만의 기록**

이다.

## 최종 서비스 문구

> **BETCHU**
> **우리 둘의 베팅 몬스터**
>
> 약속하고, 걸고, 키운다.
> 평범한 하루가 둘만의 퀘스트가 된다.
